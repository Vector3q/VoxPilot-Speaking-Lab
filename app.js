const QUESTION_BANK = {
  repeat: [
    {
      id: "lr-01",
      type: "repeat",
      category: "Campus",
      difficulty: "Easy",
      text: "The library will extend its hours during final exam week.",
      focus: "清晰复述公告句"
    },
    {
      id: "lr-02",
      type: "repeat",
      category: "Class",
      difficulty: "Easy",
      text: "Students should submit the research outline before Friday afternoon.",
      focus: "时间信息和名词短语"
    },
    {
      id: "lr-03",
      type: "repeat",
      category: "Project",
      difficulty: "Medium",
      text: "Our group decided to revise the introduction after receiving feedback.",
      focus: "过去式和长名词结构"
    },
    {
      id: "lr-04",
      type: "repeat",
      category: "Campus",
      difficulty: "Medium",
      text: "The student center offers quiet study rooms and small meeting spaces.",
      focus: "并列结构"
    },
    {
      id: "lr-05",
      type: "repeat",
      category: "Lecture",
      difficulty: "Hard",
      text: "The professor explained how environmental changes can influence animal behavior.",
      focus: "学术表达"
    },
    {
      id: "lr-06",
      type: "repeat",
      category: "Class",
      difficulty: "Medium",
      text: "I usually take notes by hand because it helps me remember details.",
      focus: "原因从句"
    },
    {
      id: "lr-07",
      type: "repeat",
      category: "Campus",
      difficulty: "Easy",
      text: "The cafeteria added several healthy options to the lunch menu.",
      focus: "日常校园词汇"
    },
    {
      id: "lr-08",
      type: "repeat",
      category: "Technology",
      difficulty: "Medium",
      text: "Online discussion boards make it easier for shy students to participate.",
      focus: "比较结构"
    },
    {
      id: "lr-09",
      type: "repeat",
      category: "Lecture",
      difficulty: "Hard",
      text: "Researchers collected data from several coastal communities over three years.",
      focus: "多音节词和数字信息"
    },
    {
      id: "lr-10",
      type: "repeat",
      category: "Project",
      difficulty: "Easy",
      text: "We need to practice the presentation at least twice before class.",
      focus: "情态动词和频率"
    },
    {
      id: "lr-11",
      type: "repeat",
      category: "Class",
      difficulty: "Medium",
      text: "The assignment asks us to compare two different theories of learning.",
      focus: "任务要求"
    },
    {
      id: "lr-12",
      type: "repeat",
      category: "Campus",
      difficulty: "Medium",
      text: "A new shuttle route connects the dormitories with the science building.",
      focus: "地点连接"
    },
    {
      id: "lr-13",
      type: "repeat",
      category: "Lecture",
      difficulty: "Hard",
      text: "Economic incentives can encourage companies to reduce energy consumption.",
      focus: "抽象名词"
    },
    {
      id: "lr-14",
      type: "repeat",
      category: "Project",
      difficulty: "Medium",
      text: "My partner suggested adding one more example to support our main argument.",
      focus: "建议表达"
    },
    {
      id: "lr-15",
      type: "repeat",
      category: "Campus",
      difficulty: "Easy",
      text: "The writing center helps students improve essays and lab reports.",
      focus: "服务介绍"
    },
    {
      id: "lr-16",
      type: "repeat",
      category: "Technology",
      difficulty: "Hard",
      text: "Digital textbooks can be updated quickly when new information becomes available.",
      focus: "被动和时间状语"
    },
    {
      id: "lr-17",
      type: "repeat",
      category: "Class",
      difficulty: "Medium",
      text: "The instructor postponed the quiz because several students were absent.",
      focus: "原因从句"
    },
    {
      id: "lr-18",
      type: "repeat",
      category: "Lecture",
      difficulty: "Hard",
      text: "Plants in dry regions often develop deep roots to reach underground water.",
      focus: "学术因果"
    }
  ],
  interview: [
    {
      id: "iv-01",
      type: "interview",
      category: "Study",
      difficulty: "Easy",
      text: "What study habit has helped you the most, and why?",
      sample: "One study habit that has helped me a lot is reviewing my notes on the same day after class. It works because the information is still fresh in my mind, so I can quickly find the parts I do not understand. For example, in my biology course, I spent fifteen minutes every evening rewriting key terms in my own words. That small routine made exam preparation much easier."
    },
    {
      id: "iv-02",
      type: "interview",
      category: "Campus",
      difficulty: "Easy",
      text: "Which campus place do you use most often?",
      sample: "I use the library most often because it gives me a quiet place to focus. My dorm room can be noisy, especially in the evening, but the library has individual desks and stable internet. Last semester, I prepared for most of my presentations there. It helped me stay organized and finish my work earlier."
    },
    {
      id: "iv-03",
      type: "interview",
      category: "Project",
      difficulty: "Medium",
      text: "Describe a time when you worked successfully with a group.",
      sample: "A successful group experience happened in my marketing class. Our team had to design a campaign for a local bookstore, and we divided the work based on each person's strengths. I handled the presentation because I was comfortable speaking. We met twice a week, shared updates, and gave each other feedback. In the end, our project was clear and well organized."
    },
    {
      id: "iv-04",
      type: "interview",
      category: "Technology",
      difficulty: "Medium",
      text: "Do you prefer online classes or in-person classes?",
      sample: "I prefer in-person classes because they make it easier for me to stay engaged. When I sit in a classroom, I can ask questions immediately and notice the professor's explanations more clearly. Online classes are convenient, but I sometimes get distracted. For example, during an online economics course, I kept checking messages. In-person classes help me focus better."
    },
    {
      id: "iv-05",
      type: "interview",
      category: "Study",
      difficulty: "Medium",
      text: "How do you manage your time when you have several deadlines?",
      sample: "When I have several deadlines, I make a short priority list and start with the task that affects my grade the most. This helps me avoid wasting time on small details. For example, during midterms, I had a paper, a quiz, and a group meeting in the same week. I finished the paper outline first, then used shorter blocks of time for review."
    },
    {
      id: "iv-06",
      type: "interview",
      category: "Class",
      difficulty: "Hard",
      text: "What kind of teacher helps you learn most effectively?",
      sample: "A teacher who explains ideas with examples helps me learn most effectively. Abstract concepts are easier to remember when I can connect them to real situations. For instance, my psychology teacher used everyday decision-making to explain cognitive bias. That example made the theory clear and memorable. I also appreciate teachers who give specific feedback after assignments."
    },
    {
      id: "iv-07",
      type: "interview",
      category: "Campus",
      difficulty: "Easy",
      text: "Would you join a student club? Why or why not?",
      sample: "Yes, I would join a student club because it is a practical way to meet people with similar interests. A club also gives me a reason to practice communication outside class. For example, I once joined a reading club, and every week I had to share one opinion about an article. That experience made me more confident."
    },
    {
      id: "iv-08",
      type: "interview",
      category: "Project",
      difficulty: "Medium",
      text: "What makes a presentation effective?",
      sample: "An effective presentation needs a clear structure and one or two memorable examples. If the audience can follow the main idea from the beginning, they are more likely to stay interested. For example, I usually start with the problem, explain the evidence, and end with a practical suggestion. Visuals are useful, but they should support the message."
    },
    {
      id: "iv-09",
      type: "interview",
      category: "Study",
      difficulty: "Hard",
      text: "Is it better to study alone or with classmates?",
      sample: "I think it is better to combine both methods, but I usually start alone. Studying alone helps me understand the basic material without pressure. After that, meeting classmates is useful because we can test each other's understanding. For example, before a history exam, I reviewed the timeline by myself and then discussed the causes of major events with friends."
    },
    {
      id: "iv-10",
      type: "interview",
      category: "Campus",
      difficulty: "Medium",
      text: "What campus service should universities improve?",
      sample: "Universities should improve academic advising because students often need clear guidance when choosing courses. A good adviser can help students avoid schedule conflicts and plan for internships. For example, one of my friends took two advanced classes in the same semester and felt overwhelmed. Better advising could help students make more balanced decisions."
    },
    {
      id: "iv-11",
      type: "interview",
      category: "Technology",
      difficulty: "Hard",
      text: "How can technology improve language learning?",
      sample: "Technology can improve language learning by giving students more chances to practice and receive quick feedback. For example, speech recognition tools can show whether a learner is speaking clearly, and online videos expose students to different accents. However, technology works best when students use it actively instead of only watching content."
    },
    {
      id: "iv-12",
      type: "interview",
      category: "Class",
      difficulty: "Medium",
      text: "Describe a course that you would recommend to other students.",
      sample: "I would recommend an introductory public speaking course because it is useful for almost every major. The course teaches students how to organize ideas, control nervousness, and explain information clearly. When I took it, I had to give three short speeches. By the end of the semester, I felt more comfortable speaking in front of classmates."
    },
    {
      id: "iv-13",
      type: "interview",
      category: "Study",
      difficulty: "Easy",
      text: "What do you do when you do not understand a lecture?",
      sample: "When I do not understand a lecture, I first review my notes and mark the confusing parts. Then I check the textbook or ask a classmate. If I am still confused, I visit the professor during office hours. This step-by-step approach helps me solve the problem without waiting until the exam."
    },
    {
      id: "iv-14",
      type: "interview",
      category: "Campus",
      difficulty: "Hard",
      text: "Should universities require students to take courses outside their major?",
      sample: "Yes, universities should require some courses outside a student's major because they build broader thinking. A computer science student, for example, may benefit from a writing course because clear communication is important in technical work. These courses also help students discover new interests. The requirement should be flexible, but I think the idea is valuable."
    },
    {
      id: "iv-15",
      type: "interview",
      category: "Project",
      difficulty: "Easy",
      text: "How do you prepare for an important presentation?",
      sample: "I prepare for an important presentation by making a simple outline first. Then I practice the opening and conclusion several times because those parts shape the audience's impression. I also record myself to check my speed. For one class presentation, this method helped me sound more confident and avoid reading directly from my slides."
    },
    {
      id: "iv-16",
      type: "interview",
      category: "Technology",
      difficulty: "Medium",
      text: "Do you think students should use AI tools for schoolwork?",
      sample: "Students can use AI tools for schoolwork if they use them responsibly. AI is helpful for brainstorming, checking grammar, or finding clearer ways to express an idea. However, students should still do the thinking themselves. For example, I might ask an AI tool to explain a concept, but I would write my own answer in my own words."
    }
  ]
};

const EXTRA_REPEAT_QUESTIONS = [
  {
    id: "lr-19",
    type: "repeat",
    category: "Campus",
    difficulty: "Easy",
    text: "The bookstore will offer discounts on used textbooks this week.",
    focus: "校园服务和时间信息"
  },
  {
    id: "lr-20",
    type: "repeat",
    category: "Class",
    difficulty: "Easy",
    text: "Please bring your draft to the writing workshop tomorrow.",
    focus: "课堂提醒"
  },
  {
    id: "lr-21",
    type: "repeat",
    category: "Campus",
    difficulty: "Medium",
    text: "The health center recommends making appointments through the online portal.",
    focus: "校园机构和动名词"
  },
  {
    id: "lr-22",
    type: "repeat",
    category: "Project",
    difficulty: "Medium",
    text: "Our team needs stronger evidence before we finalize the conclusion.",
    focus: "项目讨论"
  },
  {
    id: "lr-23",
    type: "repeat",
    category: "Lecture",
    difficulty: "Hard",
    text: "The experiment showed that memory improves when information is organized into patterns.",
    focus: "学术实验描述"
  },
  {
    id: "lr-24",
    type: "repeat",
    category: "Technology",
    difficulty: "Medium",
    text: "Students can reserve computer labs for group projects after six o'clock.",
    focus: "地点和时间"
  },
  {
    id: "lr-25",
    type: "repeat",
    category: "Class",
    difficulty: "Easy",
    text: "The professor uploaded the lecture slides before the review session.",
    focus: "过去式和课堂材料"
  },
  {
    id: "lr-26",
    type: "repeat",
    category: "Campus",
    difficulty: "Medium",
    text: "The career office is hosting a resume clinic for international students.",
    focus: "校园活动"
  },
  {
    id: "lr-27",
    type: "repeat",
    category: "Lecture",
    difficulty: "Hard",
    text: "Urban planners often consider transportation access when designing public spaces.",
    focus: "学术职业词汇"
  },
  {
    id: "lr-28",
    type: "repeat",
    category: "Project",
    difficulty: "Easy",
    text: "We should divide the survey questions into three clear sections.",
    focus: "小组安排"
  },
  {
    id: "lr-29",
    type: "repeat",
    category: "Technology",
    difficulty: "Medium",
    text: "The new app sends reminders when assignments are almost due.",
    focus: "技术工具"
  },
  {
    id: "lr-30",
    type: "repeat",
    category: "Class",
    difficulty: "Medium",
    text: "Attendance is optional, but the review session will cover key exam topics.",
    focus: "转折和考试信息"
  },
  {
    id: "lr-31",
    type: "repeat",
    category: "Campus",
    difficulty: "Easy",
    text: "The music department is moving its concert to the main auditorium.",
    focus: "地点变化"
  },
  {
    id: "lr-32",
    type: "repeat",
    category: "Lecture",
    difficulty: "Hard",
    text: "Cultural traditions can shape how communities respond to environmental challenges.",
    focus: "抽象学术表达"
  },
  {
    id: "lr-33",
    type: "repeat",
    category: "Study",
    difficulty: "Easy",
    text: "I review vocabulary every morning before I start my homework.",
    focus: "学习习惯"
  },
  {
    id: "lr-34",
    type: "repeat",
    category: "Project",
    difficulty: "Medium",
    text: "The data table is useful, but it needs a clearer explanation.",
    focus: "评价和转折"
  },
  {
    id: "lr-35",
    type: "repeat",
    category: "Campus",
    difficulty: "Medium",
    text: "The dining hall will close early because of a staff training event.",
    focus: "公告原因"
  },
  {
    id: "lr-36",
    type: "repeat",
    category: "Class",
    difficulty: "Hard",
    text: "Students are expected to analyze the author's argument rather than summarize the plot.",
    focus: "学术任务要求"
  },
  {
    id: "lr-37",
    type: "repeat",
    category: "Technology",
    difficulty: "Easy",
    text: "The classroom projector stopped working during the first presentation.",
    focus: "课堂设备"
  },
  {
    id: "lr-38",
    type: "repeat",
    category: "Lecture",
    difficulty: "Medium",
    text: "The speaker compared traditional farming methods with modern irrigation systems.",
    focus: "比较结构"
  },
  {
    id: "lr-39",
    type: "repeat",
    category: "Campus",
    difficulty: "Easy",
    text: "The gym offers free fitness classes on Saturday mornings.",
    focus: "校园活动"
  },
  {
    id: "lr-40",
    type: "repeat",
    category: "Class",
    difficulty: "Medium",
    text: "The syllabus explains how participation will affect the final grade.",
    focus: "课程文件"
  },
  {
    id: "lr-41",
    type: "repeat",
    category: "Project",
    difficulty: "Hard",
    text: "We changed our research question after discovering that the original topic was too broad.",
    focus: "项目调整"
  },
  {
    id: "lr-42",
    type: "repeat",
    category: "Study",
    difficulty: "Medium",
    text: "Taking short breaks helps me stay focused during long study sessions.",
    focus: "学习方法"
  },
  {
    id: "lr-43",
    type: "repeat",
    category: "Campus",
    difficulty: "Hard",
    text: "The university plans to renovate the oldest residence hall over the summer break.",
    focus: "校园建设公告"
  },
  {
    id: "lr-44",
    type: "repeat",
    category: "Lecture",
    difficulty: "Medium",
    text: "The article describes how social networks influence consumer decisions.",
    focus: "学术概念"
  },
  {
    id: "lr-45",
    type: "repeat",
    category: "Technology",
    difficulty: "Hard",
    text: "Video conferencing allows guest speakers to join seminars from different countries.",
    focus: "技术和学术场景"
  },
  {
    id: "lr-46",
    type: "repeat",
    category: "Class",
    difficulty: "Easy",
    text: "The quiz will include material from chapters four and five.",
    focus: "考试范围"
  },
  {
    id: "lr-47",
    type: "repeat",
    category: "Campus",
    difficulty: "Medium",
    text: "Students complained that the new parking policy was difficult to understand.",
    focus: "校园政策"
  },
  {
    id: "lr-48",
    type: "repeat",
    category: "Lecture",
    difficulty: "Hard",
    text: "The historian argued that trade routes helped spread new ideas across regions.",
    focus: "历史学术表达"
  },
  {
    id: "lr-49",
    type: "repeat",
    category: "Project",
    difficulty: "Medium",
    text: "Our survey results support the main point of the presentation.",
    focus: "项目证据"
  },
  {
    id: "lr-50",
    type: "repeat",
    category: "Study",
    difficulty: "Easy",
    text: "I prefer studying in the morning when the campus is quiet.",
    focus: "偏好表达"
  },
  {
    id: "lr-51",
    type: "repeat",
    category: "Campus",
    difficulty: "Hard",
    text: "The financial aid office requires additional documents before approving the application.",
    focus: "行政流程"
  },
  {
    id: "lr-52",
    type: "repeat",
    category: "Technology",
    difficulty: "Medium",
    text: "The online platform stores recordings of lectures for students who miss class.",
    focus: "线上学习"
  },
  {
    id: "lr-53",
    type: "repeat",
    category: "Class",
    difficulty: "Medium",
    text: "The discussion section gives students a chance to ask detailed questions.",
    focus: "课堂活动"
  },
  {
    id: "lr-54",
    type: "repeat",
    category: "Lecture",
    difficulty: "Hard",
    text: "Researchers examined how sleep quality affects concentration and academic performance.",
    focus: "研究句型"
  },
  {
    id: "lr-55",
    type: "repeat",
    category: "Campus",
    difficulty: "Easy",
    text: "The student newspaper is looking for volunteer photographers this semester.",
    focus: "社团招募"
  },
  {
    id: "lr-56",
    type: "repeat",
    category: "Project",
    difficulty: "Medium",
    text: "The final report should include an abstract, a method section, and references.",
    focus: "报告结构"
  },
  {
    id: "lr-57",
    type: "repeat",
    category: "Study",
    difficulty: "Hard",
    text: "Effective note-taking requires selecting important ideas instead of copying every sentence.",
    focus: "学习策略"
  },
  {
    id: "lr-58",
    type: "repeat",
    category: "Campus",
    difficulty: "Medium",
    text: "The international office organizes orientation sessions for newly admitted students.",
    focus: "校园服务"
  },
  {
    id: "lr-59",
    type: "repeat",
    category: "Lecture",
    difficulty: "Hard",
    text: "The lecturer emphasized the connection between economic growth and public health.",
    focus: "抽象关系"
  },
  {
    id: "lr-60",
    type: "repeat",
    category: "Class",
    difficulty: "Easy",
    text: "The teacher asked everyone to post one question before class.",
    focus: "课堂任务"
  }
];

const EXTRA_INTERVIEW_QUESTIONS = [
  {
    id: "iv-17",
    type: "interview",
    category: "Study",
    difficulty: "Easy",
    text: "What is one skill you want to improve this semester?",
    sample: "One skill I want to improve this semester is public speaking. I can explain ideas clearly in writing, but I still feel nervous when I speak in front of a group. To improve, I plan to volunteer for short class presentations and record myself after practice. That should help me become more confident and organized."
  },
  {
    id: "iv-18",
    type: "interview",
    category: "Campus",
    difficulty: "Medium",
    text: "Should universities keep libraries open late at night?",
    sample: "Yes, universities should keep libraries open late because many students have different schedules. Some students work during the day, while others focus better in the evening. For example, when I had two exams in one week, a quiet late-night study space helped me review without distractions. The university could limit late hours to exam periods to control costs."
  },
  {
    id: "iv-19",
    type: "interview",
    category: "Class",
    difficulty: "Medium",
    text: "Do you learn better from lectures or from discussions?",
    sample: "I learn better from discussions because they force me to use the information actively. Lectures are helpful for getting the basic explanation, but discussions show whether I really understand the idea. For example, in a sociology class, I remembered a theory much better after debating a case with classmates. That interaction made the concept more concrete."
  },
  {
    id: "iv-20",
    type: "interview",
    category: "Technology",
    difficulty: "Easy",
    text: "What technology do you use most often for studying?",
    sample: "I use a note-taking app most often because it keeps my course materials organized. I can create folders for each class and search for keywords before exams. For instance, when I reviewed for a history test, I quickly found all my notes about one time period. It saves time and helps me study more efficiently."
  },
  {
    id: "iv-21",
    type: "interview",
    category: "Project",
    difficulty: "Hard",
    text: "How should a group handle a member who does not finish assigned work?",
    sample: "A group should handle the problem directly but respectfully. First, the team should ask whether the member is facing a problem, because sometimes there is a reasonable explanation. Then the group can set a smaller deadline and make responsibilities clear. In one project, this approach helped our teammate catch up without creating conflict."
  },
  {
    id: "iv-22",
    type: "interview",
    category: "Campus",
    difficulty: "Easy",
    text: "What kind of campus event would you like to attend?",
    sample: "I would like to attend a career panel because it connects classroom learning with real jobs. Students can hear how graduates chose their careers and what skills they use every day. For example, a panel with former business students could help me understand internships better. It would also make career planning feel more realistic."
  },
  {
    id: "iv-23",
    type: "interview",
    category: "Study",
    difficulty: "Medium",
    text: "Is it useful to make a study schedule?",
    sample: "Yes, making a study schedule is useful because it turns a large amount of work into smaller tasks. Without a schedule, I may spend too much time on one subject and ignore another. For example, before finals, I usually assign each course a specific review block. This helps me stay calm and make steady progress."
  },
  {
    id: "iv-24",
    type: "interview",
    category: "Class",
    difficulty: "Hard",
    text: "Should professors allow students to choose their own research topics?",
    sample: "Professors should allow some choice because students work harder when the topic interests them. However, the topic still needs to fit the course goals. For example, in a writing class, my teacher gave us a broad theme but let us choose the specific issue. That freedom made the research process more meaningful and personal."
  },
  {
    id: "iv-25",
    type: "interview",
    category: "Campus",
    difficulty: "Medium",
    text: "What is one improvement you would make to a university cafeteria?",
    sample: "I would improve the cafeteria by adding clearer nutrition labels. Students often eat there several times a week, so they should understand what they are choosing. For example, labels for protein, sugar, and common allergens would help students make healthier decisions. This change would not require a new building, but it would make the service more useful."
  },
  {
    id: "iv-26",
    type: "interview",
    category: "Technology",
    difficulty: "Hard",
    text: "Should students be allowed to use laptops in every class?",
    sample: "Students should be allowed to use laptops in most classes, but teachers can set limits when needed. Laptops are useful for notes, research, and accessing course materials. However, they can also distract students. In my opinion, a teacher could allow laptops for structured tasks and ask students to close them during discussions or presentations."
  },
  {
    id: "iv-27",
    type: "interview",
    category: "Study",
    difficulty: "Easy",
    text: "What do you usually do before an important exam?",
    sample: "Before an important exam, I review my notes and make a short list of topics I still find difficult. Then I spend more time on those weak areas instead of reading everything again. For example, before a statistics exam, I practiced the formulas that confused me most. This method makes my review more focused."
  },
  {
    id: "iv-28",
    type: "interview",
    category: "Campus",
    difficulty: "Hard",
    text: "Should first-year students live on campus?",
    sample: "I think first-year students should live on campus if possible because it helps them adjust to university life. Living close to classrooms, libraries, and student activities makes it easier to build routines. For example, students can attend study groups or campus events without long travel time. It also helps them meet classmates more naturally."
  },
  {
    id: "iv-29",
    type: "interview",
    category: "Class",
    difficulty: "Medium",
    text: "Do you prefer classes with many small assignments or one final exam?",
    sample: "I prefer classes with many small assignments because they encourage steady learning. A single final exam can create too much pressure and may not show what a student knows over time. For example, weekly response papers helped me understand readings gradually in a literature course. They also gave me feedback before the final project."
  },
  {
    id: "iv-30",
    type: "interview",
    category: "Project",
    difficulty: "Easy",
    text: "What role do you usually take in a group project?",
    sample: "I usually take the role of organizer in a group project. I like making timelines, checking progress, and reminding people about deadlines. For example, in a class presentation, I created a shared document and divided the slides into sections. This helped the group work more smoothly and finish before the deadline."
  },
  {
    id: "iv-31",
    type: "interview",
    category: "Study",
    difficulty: "Hard",
    text: "Is memorization an important part of learning?",
    sample: "Memorization is important, but it should not be the only part of learning. Some basic facts, formulas, or vocabulary need to be remembered before students can use them. However, deeper learning happens when students apply those facts to problems. For example, memorizing vocabulary helped me read faster, but using the words in speaking helped me remember them longer."
  },
  {
    id: "iv-32",
    type: "interview",
    category: "Campus",
    difficulty: "Medium",
    text: "Would you rather study in a quiet room or a busy cafe?",
    sample: "I would rather study in a quiet room because I need silence to concentrate on difficult material. A busy cafe is fine for simple tasks, but conversations and music can distract me. For example, when I write an essay, I need to think carefully about structure. A quiet room helps me stay focused for a longer time."
  },
  {
    id: "iv-33",
    type: "interview",
    category: "Technology",
    difficulty: "Medium",
    text: "How can online videos help students learn?",
    sample: "Online videos can help students learn by explaining difficult ideas in a visual way. Sometimes a diagram or demonstration is clearer than a textbook paragraph. For example, when I did not understand a math concept, a short video showed the steps slowly. Videos are especially useful when students can pause and replay important parts."
  },
  {
    id: "iv-34",
    type: "interview",
    category: "Class",
    difficulty: "Easy",
    text: "What kind of class activity helps you participate more?",
    sample: "Small group discussion helps me participate more because it feels less stressful than speaking to the whole class. I can test my ideas with a few classmates first. For example, in an English class, discussing a reading in pairs helped me prepare one clear point. After that, I felt ready to share with everyone."
  },
  {
    id: "iv-35",
    type: "interview",
    category: "Campus",
    difficulty: "Hard",
    text: "Should universities require students to attend orientation programs?",
    sample: "Yes, universities should require orientation programs because new students often need practical information. Orientation can explain academic rules, campus services, and safety resources. For example, a session about the library or advising office can save students a lot of confusion later. The program should be short and useful, not just a long series of speeches."
  },
  {
    id: "iv-36",
    type: "interview",
    category: "Study",
    difficulty: "Medium",
    text: "What do you do when you lose motivation to study?",
    sample: "When I lose motivation to study, I break the task into a very small goal. Starting with ten minutes feels easier than trying to finish everything at once. For example, if I do not want to write an essay, I first make a simple outline. Once I begin, it is usually easier to continue."
  },
  {
    id: "iv-37",
    type: "interview",
    category: "Project",
    difficulty: "Medium",
    text: "Is it better for group members to choose their own tasks?",
    sample: "It is usually better for group members to choose their own tasks because people are more responsible when the task matches their strengths. However, the group still needs to make sure the work is balanced. For example, one person may design slides while another collects sources. Clear choices can make teamwork more efficient."
  },
  {
    id: "iv-38",
    type: "interview",
    category: "Class",
    difficulty: "Hard",
    text: "Should students be graded on class participation?",
    sample: "Students should be graded on participation, but the standard should be flexible. Participation encourages students to prepare and engage with the class. However, not everyone feels comfortable speaking often, so teachers should also count written questions or small group work. This makes the grade fairer while still rewarding active learning."
  },
  {
    id: "iv-39",
    type: "interview",
    category: "Campus",
    difficulty: "Easy",
    text: "What is one campus rule that students should follow carefully?",
    sample: "Students should follow library quiet-area rules carefully because many people depend on those spaces to study. If a few students talk loudly, it affects everyone nearby. For example, during exam week, quiet study rooms are very important for concentration. Respecting that rule shows consideration for other students."
  },
  {
    id: "iv-40",
    type: "interview",
    category: "Technology",
    difficulty: "Hard",
    text: "Can social media be useful for academic work?",
    sample: "Social media can be useful for academic work if students use it with a clear purpose. It can help them follow experts, find event announcements, or discuss course topics. For example, a class group can share reminders and useful articles. The problem is distraction, so students need boundaries when using it for school."
  },
  {
    id: "iv-41",
    type: "interview",
    category: "Study",
    difficulty: "Medium",
    text: "Do you prefer handwritten notes or digital notes?",
    sample: "I prefer handwritten notes when the material is difficult because writing by hand slows me down in a helpful way. I have to choose the most important points instead of copying everything. For example, in a philosophy course, handwritten notes helped me organize arguments more clearly. Digital notes are convenient, but handwriting helps me remember better."
  },
  {
    id: "iv-42",
    type: "interview",
    category: "Campus",
    difficulty: "Medium",
    text: "What should students consider when choosing a dormitory?",
    sample: "Students should consider location, noise level, and access to study spaces when choosing a dormitory. A dorm may look nice, but if it is far from classes, daily life can become stressful. For example, I would prefer a simple room near the library over a larger room far away. Convenience affects study habits."
  },
  {
    id: "iv-43",
    type: "interview",
    category: "Class",
    difficulty: "Easy",
    text: "What makes homework useful?",
    sample: "Homework is useful when it helps students practice what they learned in class. It should not just be busy work. For example, after a grammar lesson, a short writing task can show whether students can use the grammar correctly. Useful homework also gives teachers a chance to see where students are struggling."
  },
  {
    id: "iv-44",
    type: "interview",
    category: "Project",
    difficulty: "Hard",
    text: "How can a group make a final presentation more convincing?",
    sample: "A group can make a final presentation more convincing by using clear evidence and a simple structure. The audience should understand the problem, the evidence, and the conclusion without getting lost. For example, instead of showing many complicated charts, a group could choose two important results and explain why they matter."
  },
  {
    id: "iv-45",
    type: "interview",
    category: "Technology",
    difficulty: "Medium",
    text: "Would you take a class that uses virtual reality?",
    sample: "Yes, I would take a class that uses virtual reality if it helps explain the subject. Some topics are easier to understand when students can experience a scene or process visually. For example, architecture students could examine a building design from inside. The technology should support learning, not replace careful explanation."
  },
  {
    id: "iv-46",
    type: "interview",
    category: "Campus",
    difficulty: "Hard",
    text: "Should universities offer more mental health workshops?",
    sample: "Universities should offer more mental health workshops because students often face stress from exams, social life, and future planning. Workshops can teach practical skills like time management, sleep habits, and stress reduction. For example, a short workshop before finals could help students manage pressure. These services make campus life healthier."
  },
  {
    id: "iv-47",
    type: "interview",
    category: "Study",
    difficulty: "Easy",
    text: "What is one mistake students make when preparing for exams?",
    sample: "One common mistake is waiting until the last minute to study. This creates stress and makes it hard to understand difficult material. For example, if a student starts reviewing the night before an exam, they may memorize facts but forget them quickly. Studying a little each day is more effective."
  },
  {
    id: "iv-48",
    type: "interview",
    category: "Class",
    difficulty: "Medium",
    text: "Do you like receiving feedback from classmates?",
    sample: "I like receiving feedback from classmates because they often notice problems that I miss. Their comments can be more direct and easier to understand than formal teacher feedback. For example, a classmate once told me that my presentation introduction was too long. After I shortened it, my main point became clearer."
  },
  {
    id: "iv-49",
    type: "interview",
    category: "Campus",
    difficulty: "Medium",
    text: "Should students have part-time jobs during university?",
    sample: "Students can have part-time jobs during university if the work schedule does not hurt their studies. A job can teach responsibility and help students manage money. For example, working ten hours a week at the campus library might be reasonable. However, too many hours can reduce study time and increase stress."
  },
  {
    id: "iv-50",
    type: "interview",
    category: "Study",
    difficulty: "Hard",
    text: "Is curiosity more important than discipline for successful learning?",
    sample: "Curiosity and discipline are both important, but discipline is slightly more important for long-term learning. Curiosity helps students start exploring a topic, while discipline helps them continue when the material becomes difficult. For example, I may feel curious about economics at first, but regular reading and practice are what actually improve my understanding."
  }
];

QUESTION_BANK.repeat.push(...EXTRA_REPEAT_QUESTIONS);
QUESTION_BANK.interview.push(...EXTRA_INTERVIEW_QUESTIONS);

const STORAGE_KEY = "toefl-speaking-lab-history-v1";
const SETUP_STORAGE_KEY = "toefl-speaking-lab-setup-v1";
const SpeechRecognitionApi = window.SpeechRecognition || window.webkitSpeechRecognition;

const state = {
  tab: "practice",
  mode: "repeat",
  currentRepeat: QUESTION_BANK.repeat[0],
  currentInterview: QUESTION_BANK.interview[0],
  showSentence: false,
  transcript: "",
  feedback: null,
  isRecording: false,
  status: "准备开始",
  elapsed: 0,
  duration: 0,
  audioUrl: "",
  audioBlob: null,
  isScoring: false,
  audioStats: null,
  audioAnalysisPending: false,
  recognitionStats: {
    confidenceSamples: [],
    finalSegments: 0,
    interimSegments: 0
  },
  mediaRecorder: null,
  stream: null,
  recognition: null,
  timerId: null,
  lastSavedSignature: "",
  setup: loadSetupConfig(),
  bankType: "all",
  bankDifficulty: "all",
  toast: "",
  full: {
    active: false,
    complete: false,
    examMode: true,
    sequence: [],
    index: 0,
    results: []
  }
};

const app = document.querySelector("#app");

function init() {
  state.currentRepeat = randomItem(QUESTION_BANK.repeat);
  state.currentInterview = randomItem(QUESTION_BANK.interview);
  render();
}

function render() {
  app.innerHTML = `
    <div class="app-shell">
      ${renderTopbar()}
      ${renderNav()}
      <main class="content">
        ${renderActiveTab()}
      </main>
      ${state.toast ? `<div class="toast">${escapeHtml(state.toast)}</div>` : ""}
    </div>
  `;
  bindEvents();
  updateTimerDom();
}

function renderTopbar() {
  const history = loadHistory();
  const average = averageScore(history);
  const todayCount = history.filter((item) => isToday(item.createdAt)).length;
  return `
    <header class="topbar">
      <div class="brand">
        <div class="brand-mark">TS</div>
        <div>
          <h1 class="brand-title">VoxPilot Speaking Lab</h1>
          <p class="brand-subtitle">2026 口语练习台 · 本地保存 · 无需登录</p>
        </div>
      </div>
      <div class="top-actions">
        <span class="stat-pill"><strong>${todayCount}</strong> 今日练习</span>
        <span class="stat-pill"><strong>${average || "-"}</strong> 近况均分</span>
        <span class="stat-pill"><strong>${history.length}</strong> 本地记录</span>
        <span class="stat-pill"><strong>${QUESTION_BANK.repeat.length + QUESTION_BANK.interview.length}</strong> 仿真题</span>
      </div>
    </header>
  `;
}

function renderNav() {
  const tabs = [
    ["practice", "练习台"],
    ["full", "完整练习"],
    ["bank", "题库"],
    ["history", "历史"],
    ["setup", "Setup"]
  ];
  return `
    <nav class="main-nav" aria-label="Primary navigation">
      ${tabs
        .map(
          ([id, label]) => `
            <button class="nav-button ${state.tab === id ? "active" : ""}" data-tab="${id}">
              ${label}
            </button>
          `
        )
        .join("")}
    </nav>
  `;
}

function renderActiveTab() {
  if (state.tab === "full") return renderFullRunView();
  if (state.tab === "bank") return renderBankView();
  if (state.tab === "history") return renderHistoryView();
  if (state.tab === "setup") return renderSetupView();
  return renderPracticeView();
}

function renderPracticeView() {
  return `
    <section class="workspace">
      <aside class="panel">
        <h2>今日训练</h2>
        <p class="compact-copy">先听准，再说稳。每题完成后立刻看反馈，然后重录一次。</p>
        <div class="segmented" role="group" aria-label="Practice mode">
          <button class="${state.mode === "repeat" ? "active" : ""}" data-mode="repeat">Listen & Repeat</button>
          <button class="${state.mode === "interview" ? "active" : ""}" data-mode="interview">Interview</button>
        </div>
        ${renderMiniStats()}
      </aside>
      <div>
        ${renderPracticeCard(false)}
        ${renderFeedback()}
      </div>
    </section>
  `;
}

function renderMiniStats() {
  const history = loadHistory();
  const repeatItems = history.filter((item) => item.type === "repeat");
  const interviewItems = history.filter((item) => item.type === "interview");
  return `
    <div class="mini-stats">
      <div class="mini-stat">
        <span>复述均分</span>
        <strong>${averageScore(repeatItems) || "-"}</strong>
      </div>
      <div class="mini-stat">
        <span>问答均分</span>
        <strong>${averageScore(interviewItems) || "-"}</strong>
      </div>
      <div class="mini-stat">
        <span>最近题数</span>
        <strong>${history.slice(0, 10).length}</strong>
      </div>
      <div class="mini-stat">
        <span>连续目标</span>
        <strong>${history.length ? "再练" : "开始"}</strong>
      </div>
    </div>
  `;
}

function renderPracticeCard(isFullRun) {
  const question = getActiveQuestion(isFullRun);
  const isRepeat = question.type === "repeat";
  const examLocked = isFullRun && state.full.examMode;
  const text = isRepeat && !state.showSentence ? "播放句子后复述，完成后再显示原句。" : question.text;
  const difficultyClass = question.difficulty === "Hard" ? "orange" : question.difficulty === "Medium" ? "blue" : "";
  const fullProgress = isFullRun ? renderFullProgress() : "";

  return `
    <section class="practice-board">
      ${fullProgress}
      <div class="practice-header">
        <div>
          <div class="badge-row">
            <span class="badge">${isRepeat ? "Listen and Repeat" : "Take an Interview"}</span>
            <span class="badge blue">${escapeHtml(question.category)}</span>
            <span class="badge ${difficultyClass}">${escapeHtml(question.difficulty)}</span>
          </div>
          <h2 class="question-text ${isRepeat ? "sentence-text" : ""} ${isRepeat && !state.showSentence ? "blurred" : ""}">
            ${escapeHtml(text)}
          </h2>
          <p class="prompt-note">
            ${isRepeat ? escapeHtml(question.focus) : "建议 35-60 秒：直接回答、给理由、给例子、收束观点。"}
          </p>
        </div>
        <div class="question-actions">
          ${isRepeat && !examLocked ? `<button class="ghost-button" data-show-sentence="true"><span class="button-icon">Aa</span>${state.showSentence ? "隐藏原句" : "显示原句"}</button>` : ""}
          <button class="ghost-button" data-play-prompt="true"><span class="button-icon">▶</span>${isRepeat ? "播放" : "朗读题目"}</button>
          ${!examLocked ? `<button class="ghost-button" data-next-question="true"><span class="button-icon">↻</span>${isFullRun ? "换本题" : "下一题"}</button>` : ""}
        </div>
      </div>

      <div class="control-band">
        <div class="record-control">
          <button class="record-button ${state.isRecording ? "recording" : ""}" data-record-toggle="true" aria-label="${state.isRecording ? "停止录音" : "开始录音"}">
            <span class="record-dot"></span>
          </button>
          <span class="timer" id="timer">${formatTime(state.elapsed)}</span>
        </div>
        <div>
          <div class="control-actions">
            <button class="primary-button" data-record-toggle="true">
              <span class="button-icon">${state.isRecording ? "■" : "●"}</span>
              ${state.isRecording ? "停止录音" : "开始录音"}
            </button>
            <button class="tool-button" data-analyze="true"><span class="button-icon">✓</span>分析</button>
            <button class="tool-button" data-clear-current="true"><span class="button-icon">×</span>清空</button>
            ${isFullRun && state.feedback ? `<button class="tool-button" data-full-next="true"><span class="button-icon">→</span>${state.full.index === state.full.sequence.length - 1 ? "完成" : "下一题"}</button>` : ""}
          </div>
          <div class="status-line">
            <span class="status-dot ${state.isRecording ? "recording" : state.feedback ? "done" : "ready"}"></span>
            <span>${escapeHtml(state.status)}</span>
          </div>
          ${state.audioUrl ? `<audio class="audio-player" controls src="${state.audioUrl}"></audio>` : ""}
        </div>
      </div>

      <div class="transcript-area">
        <label for="transcriptInput">转写文本</label>
        <textarea id="transcriptInput" placeholder="${SpeechRecognitionApi ? "录音时会自动生成，也可以手动修改。" : "当前浏览器不支持自动识别，请在这里输入你刚才说的内容。"}">${escapeHtml(state.transcript)}</textarea>
      </div>
    </section>
  `;
}

function renderFullProgress() {
  const total = state.full.sequence.length || 11;
  const current = Math.min(state.full.index + 1, total);
  const percent = total ? Math.round((state.full.index / total) * 100) : 0;
  return `
    <div class="badge-row">
      <span class="badge violet">完整练习 ${current}/${total}</span>
      <span class="badge blue">约 8 分钟</span>
    </div>
    <div class="progress-track" aria-label="Full practice progress">
      <div class="progress-fill" style="width: ${percent}%"></div>
    </div>
    <div style="height: 18px"></div>
  `;
}

function renderFeedback() {
  if (!state.feedback) {
    return `
      <section class="feedback-board">
        <div class="empty-feedback">${state.isScoring ? "正在生成评分报告，外部 API 可能需要几秒。" : "完成录音后，这里会显示分数、问题诊断和复练建议。"}</div>
      </section>
    `;
  }

  const feedback = state.feedback;
  const meterWidth = Math.max(0, Math.min(100, Math.round((feedback.score / 6) * 100)));
  return `
    <section class="feedback-board">
      <h2>本次反馈</h2>
      <p class="compact-copy">${escapeHtml(feedback.summary)}</p>
      <div class="feedback-grid">
        <aside class="score-box">
          <div class="score-number">${feedback.score}<span>/ 6</span></div>
          <div class="meter"><div class="meter-fill" style="width: ${meterWidth}%"></div></div>
          <div class="metric-list">
            ${feedback.metrics
              .map(
                (metric) => `
                  <div class="metric-row">
                    <span>${escapeHtml(metric.label)}</span>
                    <strong>${escapeHtml(metric.value)}</strong>
                  </div>
                `
              )
              .join("")}
          </div>
        </aside>
        <div class="feedback-columns">
          <section class="feedback-section">
            <h3>做得好的地方</h3>
            <ul>${feedback.strengths.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}</ul>
          </section>
          <section class="feedback-section">
            <h3>优先修正</h3>
            <ul>${feedback.issues.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}</ul>
          </section>
          ${feedback.detailScores ? renderScoreBreakdown(feedback.detailScores) : ""}
          ${feedback.rubricProfile ? renderRubricProfile(feedback.rubricProfile) : ""}
          ${feedback.comparison ? renderAttemptComparison(feedback.comparison) : ""}
          ${feedback.externalAi ? renderExternalAiSection(feedback.externalAi) : ""}
          ${feedback.pronunciation ? renderPronunciationSection(feedback.pronunciation) : ""}
          ${feedback.alignment ? renderDiffSection(feedback.alignment) : ""}
          <section class="feedback-section wide-section">
            <h3>${feedback.type === "repeat" ? "下一遍怎么练" : "示范升级答案"}</h3>
            <p class="upgrade-answer">${escapeHtml(feedback.improvedAnswer)}</p>
          </section>
        </div>
      </div>
    </section>
  `;
}

function renderDiffSection(alignment) {
  return `
    <section class="feedback-section wide-section">
      <h3>原句对比</h3>
      <div class="diff-line">
        ${alignment
          .map((token) => {
            if (token.type === "match") return `<span class="word-token match">${escapeHtml(token.ref)}</span>`;
            if (token.type === "substitute") return `<span class="word-token substitute">${escapeHtml(token.ref)} → ${escapeHtml(token.hyp)}</span>`;
            if (token.type === "missing") return `<span class="word-token missing">${escapeHtml(token.ref)}</span>`;
            return `<span class="word-token extra">+ ${escapeHtml(token.hyp)}</span>`;
          })
          .join("")}
      </div>
    </section>
  `;
}

function renderScoreBreakdown(detailScores) {
  return `
    <section class="feedback-section wide-section">
      <h3>评分细目</h3>
      <div class="score-breakdown">
        ${detailScores
          .map(
            (item) => `
              <div class="breakdown-row">
                <div>
                  <strong>${escapeHtml(item.label)}</strong>
                  <span>${escapeHtml(item.detail)}</span>
                </div>
                <div class="breakdown-meter" aria-label="${escapeHtml(item.label)}">
                  <div style="width: ${Math.round(item.score * 100)}%"></div>
                </div>
                <b>${Math.round(item.score * 100)}%</b>
              </div>
            `
          )
          .join("")}
      </div>
    </section>
  `;
}

function renderRubricProfile(rubricProfile) {
  return `
    <section class="feedback-section wide-section">
      <h3>考试权重</h3>
      <div class="rubric-grid">
        ${rubricProfile
          .map(
            (item) => `
              <div class="rubric-tile">
                <span>${escapeHtml(item.label)}</span>
                <strong>${Math.round(item.weight * 100)}%</strong>
                <p>${escapeHtml(item.note)}</p>
              </div>
            `
          )
          .join("")}
      </div>
    </section>
  `;
}

function renderAttemptComparison(comparison) {
  return `
    <section class="feedback-section wide-section">
      <h3>复练对比</h3>
      <div class="comparison-grid">
        <div class="comparison-score ${comparison.delta >= 0 ? "up" : "down"}">
          <span>相对上次</span>
          <strong>${comparison.delta >= 0 ? "+" : ""}${comparison.delta}</strong>
        </div>
        <div>
          <p class="upgrade-answer">${escapeHtml(comparison.summary)}</p>
          <ul class="pron-notes">
            ${comparison.notes.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}
          </ul>
        </div>
      </div>
    </section>
  `;
}

function renderExternalAiSection(externalAi) {
  const dimensions = Object.entries(externalAi.dimensionScores || {}).slice(0, 8);
  return `
    <section class="feedback-section wide-section">
      <h3>AI 评分增强</h3>
      <div class="external-grid">
        <div class="pron-tile">
          <span>AI 分数</span>
          <strong>${externalAi.score ? `${externalAi.score}/6` : "-"}</strong>
        </div>
        <div class="external-copy">
          <p class="upgrade-answer">${escapeHtml(externalAi.summary || "AI 已返回评分补充。")}</p>
          ${externalAi.grammarNotes && externalAi.grammarNotes.length ? `
            <ul class="pron-notes">
              ${externalAi.grammarNotes.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}
            </ul>
          ` : ""}
        </div>
      </div>
      ${dimensions.length ? `
        <div class="item-meta">
          ${dimensions.map(([key, value]) => `<span class="badge blue">${escapeHtml(key)} ${escapeHtml(String(value))}</span>`).join("")}
        </div>
      ` : ""}
    </section>
  `;
}

function renderPronunciationSection(pronunciation) {
  const stats = pronunciation.audioStats || {};
  const statsItems = [
    ["可识别度", `${Math.round(pronunciation.score * 100)}%`],
    ["识别置信", `${Math.round(pronunciation.confidence * 100)}%`],
    ["录音质量", `${Math.round(pronunciation.audioQuality * 100)}%`],
    ["停顿控制", `${Math.round(pronunciation.pauseControl * 100)}%`],
    ["发声语速", pronunciation.articulationRate ? `${pronunciation.articulationRate} WPM` : "-"],
    ["最长停顿", stats.available ? `${stats.longestSilence.toFixed(1)}s` : "-"],
    ["静音占比", stats.available ? `${Math.round(stats.silenceRatio * 100)}%` : "-"],
    ["估算 SNR", stats.available ? `${Math.round(stats.snrDb)} dB` : "-"]
  ];

  return `
    <section class="feedback-section wide-section">
      <h3>发音与音频诊断</h3>
      <div class="pron-grid">
        ${statsItems
          .map(
            ([label, value]) => `
              <div class="pron-tile">
                <span>${escapeHtml(label)}</span>
                <strong>${escapeHtml(value)}</strong>
              </div>
            `
          )
          .join("")}
      </div>
      <ul class="pron-notes">
        ${pronunciation.notes.map((item) => `<li>${escapeHtml(item)}</li>`).join("")}
      </ul>
      ${pronunciation.external ? renderExternalPronunciation(pronunciation.external) : ""}
      ${pronunciation.phonemeFocus && pronunciation.phonemeFocus.length ? `
        <div class="phoneme-list">
          ${pronunciation.phonemeFocus
            .map(
              (item) => `
                <div class="phoneme-card">
                  <strong>${escapeHtml(item.sound)}</strong>
                  <span>${escapeHtml(item.label)}</span>
                  <p>${escapeHtml(item.tip)}</p>
                  <small>${escapeHtml(item.words.join(", "))}</small>
                </div>
              `
            )
            .join("")}
        </div>
      ` : ""}
    </section>
  `;
}

function renderExternalPronunciation(external) {
  const stats = [
    ["Provider", external.provider || "-"],
    ["Pron", external.score !== null ? `${Math.round(external.score * 100)}%` : "-"],
    ["Accuracy", external.accuracy !== null ? `${Math.round(external.accuracy * 100)}%` : "-"],
    ["Fluency", external.fluency !== null ? `${Math.round(external.fluency * 100)}%` : "-"],
    ["Completeness", external.completeness !== null ? `${Math.round(external.completeness * 100)}%` : "-"]
  ];
  return `
    <div class="external-pron">
      <div class="pron-grid">
        ${stats
          .map(
            ([label, value]) => `
              <div class="pron-tile">
                <span>${escapeHtml(label)}</span>
                <strong>${escapeHtml(value)}</strong>
              </div>
            `
          )
          .join("")}
      </div>
      ${external.phonemes && external.phonemes.length ? `
        <div class="item-meta">
          ${external.phonemes.slice(0, 8).map((item) => `<span class="badge orange">${escapeHtml(item.word || "")} ${escapeHtml(item.sound || item.phoneme || "")} ${Number.isFinite(Number(item.score)) ? Math.round(Number(item.score)) : ""}</span>`).join("")}
        </div>
      ` : ""}
    </div>
  `;
}

function renderFullRunView() {
  if (state.full.complete) {
    return renderFullSummary();
  }

  if (!state.full.active) {
    return `
      <section class="full-run-board">
        <div class="full-setup">
          <div>
            <h2>完整练习</h2>
            <p class="compact-copy">一次完成 11 题：6 道复述，5 道面试问答。考试模式会自动朗读题目，锁定换题和显示原句。</p>
          </div>
          <button class="primary-button" data-start-full="true"><span class="button-icon">▶</span>开始完整练习</button>
        </div>
        ${renderFullTips()}
      </section>
    `;
  }

  return `
    <div>
      ${renderPracticeCard(true)}
      ${renderFeedback()}
    </div>
  `;
}

function renderFullTips() {
  return `
    <div class="summary-grid">
      <div class="summary-tile">
        <span>题量</span>
        <strong>11</strong>
      </div>
      <div class="summary-tile">
        <span>复述</span>
        <strong>6</strong>
      </div>
      <div class="summary-tile">
        <span>问答</span>
        <strong>5</strong>
      </div>
      <div class="summary-tile">
        <span>保存</span>
        <strong>本地</strong>
      </div>
    </div>
  `;
}

function renderFullSummary() {
  const results = state.full.results;
  const avg = averageScore(results.map((item) => ({ score: item.feedback.score })));
  const repeatAvg = averageScore(results.filter((item) => item.question.type === "repeat").map((item) => ({ score: item.feedback.score })));
  const interviewAvg = averageScore(results.filter((item) => item.question.type === "interview").map((item) => ({ score: item.feedback.score })));
  const commonIssues = results.flatMap((item) => item.feedback.issues).slice(0, 5);

  return `
    <section class="full-run-board">
      <div class="history-top">
        <div>
          <h2>本轮总结</h2>
          <p class="compact-copy">平均分 ${avg || "-"}。下一轮建议优先重练分数最低的两题。</p>
        </div>
        <button class="primary-button" data-start-full="true"><span class="button-icon">↻</span>再来一轮</button>
      </div>
      <div class="summary-grid">
        <div class="summary-tile"><span>总均分</span><strong>${avg || "-"}</strong></div>
        <div class="summary-tile"><span>复述均分</span><strong>${repeatAvg || "-"}</strong></div>
        <div class="summary-tile"><span>问答均分</span><strong>${interviewAvg || "-"}</strong></div>
        <div class="summary-tile"><span>完成题数</span><strong>${results.length}</strong></div>
      </div>
      <section class="feedback-section" style="margin-top: 16px">
        <h3>下次优先修正</h3>
        <ul>${(commonIssues.length ? commonIssues : ["继续保持完整回答，并在每题结束后立刻复练一次。"]).map((item) => `<li>${escapeHtml(item)}</li>`).join("")}</ul>
      </section>
    </section>
  `;
}

function renderBankView() {
  const items = getFilteredQuestions();
  return `
    <section>
      <div class="history-top">
        <div>
          <h2>题库</h2>
          <p class="compact-copy">现在共有 ${QUESTION_BANK.repeat.length + QUESTION_BANK.interview.length} 道仿真题。题目基于 ETS 公开题型说明原创编写，不包含泄露考场题。</p>
        </div>
        <button class="primary-button" data-random-bank="true"><span class="button-icon">↻</span>随机练一题</button>
      </div>
      <div class="filters">
        <select id="bankType">
          <option value="all" ${state.bankType === "all" ? "selected" : ""}>全部题型</option>
          <option value="repeat" ${state.bankType === "repeat" ? "selected" : ""}>Listen and Repeat</option>
          <option value="interview" ${state.bankType === "interview" ? "selected" : ""}>Take an Interview</option>
        </select>
        <select id="bankDifficulty">
          <option value="all" ${state.bankDifficulty === "all" ? "selected" : ""}>全部难度</option>
          <option value="Easy" ${state.bankDifficulty === "Easy" ? "selected" : ""}>Easy</option>
          <option value="Medium" ${state.bankDifficulty === "Medium" ? "selected" : ""}>Medium</option>
          <option value="Hard" ${state.bankDifficulty === "Hard" ? "selected" : ""}>Hard</option>
        </select>
      </div>
      <div class="bank-list">
        ${items
          .map(
            (item) => `
              <article class="bank-item">
                <div>
                  <p class="item-title">${escapeHtml(item.text)}</p>
                  <div class="item-meta">
                    <span class="badge">${item.type === "repeat" ? "Repeat" : "Interview"}</span>
                    <span class="badge blue">${escapeHtml(item.category)}</span>
                    <span class="badge orange">${escapeHtml(item.difficulty)}</span>
                  </div>
                </div>
                <button class="small-button" data-practice-question="${item.id}">练这题</button>
              </article>
            `
          )
          .join("")}
      </div>
    </section>
  `;
}

function renderHistoryView() {
  const history = loadHistory();
  return `
    <section>
      <div class="history-top">
        <div>
          <h2>本地历史</h2>
          <p class="compact-copy">记录保存在当前浏览器，不会上传到服务器。</p>
        </div>
        <div class="control-actions">
          <button class="ghost-button" data-export-history="true"><span class="button-icon">↓</span>导出</button>
          <button class="danger-button" data-clear-history="true"><span class="button-icon">×</span>清空</button>
        </div>
      </div>
      ${renderWeaknessPanel(history)}
      <div class="history-list">
        ${
          history.length
            ? history
                .map(
                  (item) => `
                    <article class="history-item">
                      <div>
                        <p class="item-title">${escapeHtml(item.prompt)}</p>
                        <p class="compact-copy">${escapeHtml(item.summary)}</p>
                        <div class="item-meta">
                          <span class="badge">${item.type === "repeat" ? "Repeat" : "Interview"}</span>
                          <span class="badge blue">${formatDate(item.createdAt)}</span>
                          <span class="badge orange">Score ${item.score}/6</span>
                        </div>
                      </div>
                      <button class="small-button" data-review-history="${item.id}">查看</button>
                    </article>
                  `
                )
                .join("")
            : `<div class="empty-feedback">还没有练习记录。</div>`
        }
      </div>
    </section>
  `;
}

function renderSetupView() {
  const setup = state.setup;
  return `
    <section class="setup-board">
      <div class="history-top">
        <div>
          <h2>API Setup</h2>
          <p class="compact-copy">API key、endpoint 和 model 只保存在当前浏览器 localStorage，不会写入 GitHub 仓库。</p>
        </div>
        <div class="control-actions">
          <button class="primary-button" data-save-setup="true"><span class="button-icon">✓</span>保存设置</button>
          <button class="ghost-button" data-clear-setup="true"><span class="button-icon">×</span>清空设置</button>
        </div>
      </div>

      <div class="setup-grid">
        <section class="feedback-section">
          <h3>AI 内容评分</h3>
          <label class="check-row">
            <input id="aiEnabled" type="checkbox" ${setup.ai.enabled ? "checked" : ""}>
            <span>启用外部 AI 评分增强</span>
          </label>
          <label class="setup-field">
            <span>API 类型</span>
            <select id="aiMode">
              <option value="responses" ${setup.ai.mode === "responses" ? "selected" : ""}>OpenAI Responses API</option>
              <option value="chat" ${setup.ai.mode === "chat" ? "selected" : ""}>OpenAI-compatible Chat Completions</option>
            </select>
          </label>
          <label class="setup-field">
            <span>Endpoint URL</span>
            <input id="aiEndpoint" value="${escapeHtml(setup.ai.endpoint)}" placeholder="https://api.openai.com/v1/responses">
          </label>
          <label class="setup-field">
            <span>API Key</span>
            <input id="aiKey" type="password" value="${escapeHtml(setup.ai.apiKey)}" placeholder="sk-...">
          </label>
          <label class="setup-field">
            <span>Model</span>
            <input id="aiModel" value="${escapeHtml(setup.ai.model)}" placeholder="gpt-4.1-mini">
          </label>
          <label class="setup-field">
            <span>Temperature</span>
            <input id="aiTemperature" type="number" step="0.1" min="0" max="1" value="${escapeHtml(String(setup.ai.temperature))}">
          </label>
          <p class="compact-copy">AI 会按 TOEFL 口语维度补充内容、语法、结构和示范改写。没有配置时自动使用本地评分。</p>
        </section>

        <section class="feedback-section">
          <h3>逐音素/发音评分</h3>
          <label class="check-row">
            <input id="pronEnabled" type="checkbox" ${setup.pronunciation.enabled ? "checked" : ""}>
            <span>启用外部发音评分增强</span>
          </label>
          <label class="setup-field">
            <span>Provider</span>
            <select id="pronProvider">
              <option value="azure" ${setup.pronunciation.provider === "azure" ? "selected" : ""}>Azure Speech Pronunciation Assessment</option>
              <option value="custom" ${setup.pronunciation.provider === "custom" ? "selected" : ""}>Custom JSON endpoint</option>
            </select>
          </label>
          <label class="setup-field">
            <span>Azure Region</span>
            <input id="azureRegion" value="${escapeHtml(setup.pronunciation.azureRegion)}" placeholder="eastus">
          </label>
          <label class="setup-field">
            <span>Endpoint Override</span>
            <input id="pronEndpoint" value="${escapeHtml(setup.pronunciation.endpoint)}" placeholder="留空则按 Azure region 自动生成">
          </label>
          <label class="setup-field">
            <span>API Key</span>
            <input id="pronKey" type="password" value="${escapeHtml(setup.pronunciation.apiKey)}" placeholder="Azure Speech key 或自定义 key">
          </label>
          <p class="compact-copy">Azure 模式会把录音转成 16kHz WAV 后请求 Pronunciation Assessment。Custom 模式会向你的 endpoint 发送 JSON。</p>
        </section>
      </div>

      <section class="feedback-section setup-note">
        <h3>安全说明</h3>
        <ul>
          <li>源码里没有任何个人 API key，提交到 GitHub 不会泄露你的密钥。</li>
          <li>密钥只存在当前浏览器 localStorage；换浏览器或清缓存后需要重新填写。</li>
          <li>如果部署成公开网站，用户填写的 key 仍会在自己的浏览器里发起请求，适合个人工具，不适合多人 SaaS 后台。</li>
        </ul>
      </section>
    </section>
  `;
}

function renderWeaknessPanel(history) {
  if (!history.length) return "";
  const recent = history.slice(0, 20);
  const buckets = {};
  recent.forEach((item) => {
    (item.detailScores || []).forEach((detail) => {
      if (!buckets[detail.label]) buckets[detail.label] = [];
      buckets[detail.label].push(Number(detail.score));
    });
  });
  const weakScores = Object.entries(buckets)
    .map(([label, values]) => ({
      label,
      score: values.reduce((sum, value) => sum + value, 0) / values.length
    }))
    .sort((a, b) => a.score - b.score)
    .slice(0, 4);
  const issueCounts = {};
  recent.flatMap((item) => item.issues || []).forEach((issue) => {
    const key = classifyIssue(issue);
    issueCounts[key] = (issueCounts[key] || 0) + 1;
  });
  const topIssues = Object.entries(issueCounts).sort((a, b) => b[1] - a[1]).slice(0, 4);

  return `
    <section class="feedback-section wide-section weakness-panel">
      <h3>弱项追踪</h3>
      <div class="weakness-grid">
        ${weakScores
          .map(
            (item) => `
              <div class="weakness-tile">
                <span>${escapeHtml(item.label)}</span>
                <strong>${Math.round(item.score * 100)}%</strong>
              </div>
            `
          )
          .join("")}
      </div>
      <div class="item-meta">
        ${topIssues.map(([label, count]) => `<span class="badge orange">${escapeHtml(label)} x${count}</span>`).join("")}
      </div>
    </section>
  `;
}

function classifyIssue(issue) {
  if (/漏|完整|复述/.test(issue)) return "复述完整度";
  if (/理由|例子|结果|展开/.test(issue)) return "内容展开";
  if (/组织|顺序|结构/.test(issue)) return "组织逻辑";
  if (/发音|词尾|可识别/.test(issue)) return "发音清晰度";
  if (/语速|停顿|流利/.test(issue)) return "流利度";
  if (/录音|噪音|音量/.test(issue)) return "录音质量";
  if (/词汇|重复|语言/.test(issue)) return "语言使用";
  return "综合问题";
}

function bindEvents() {
  document.querySelectorAll("[data-tab]").forEach((button) => {
    button.addEventListener("click", () => {
      state.tab = button.dataset.tab;
      render();
    });
  });

  document.querySelectorAll("[data-mode]").forEach((button) => {
    button.addEventListener("click", () => {
      state.mode = button.dataset.mode;
      clearCurrentWork(true);
      render();
    });
  });

  document.querySelectorAll("[data-record-toggle]").forEach((button) => {
    button.addEventListener("click", () => {
      if (state.isRecording) stopRecording();
      else startRecording();
    });
  });

  const transcriptInput = document.querySelector("#transcriptInput");
  if (transcriptInput) {
    transcriptInput.addEventListener("input", (event) => {
      state.transcript = event.target.value;
    });
  }

  const bankType = document.querySelector("#bankType");
  if (bankType) {
    bankType.addEventListener("change", (event) => {
      state.bankType = event.target.value;
      render();
    });
  }

  const bankDifficulty = document.querySelector("#bankDifficulty");
  if (bankDifficulty) {
    bankDifficulty.addEventListener("change", (event) => {
      state.bankDifficulty = event.target.value;
      render();
    });
  }

  bindClick("[data-show-sentence]", () => {
    state.showSentence = !state.showSentence;
    render();
  });
  bindClick("[data-play-prompt]", () => speakPrompt());
  bindClick("[data-next-question]", () => nextQuestion());
  bindClick("[data-analyze]", () => analyzeCurrent());
  bindClick("[data-clear-current]", () => {
    clearCurrentWork(false);
    render();
  });
  bindClick("[data-start-full]", () => startFullRun());
  bindClick("[data-full-next]", () => nextFullItem());
  bindClick("[data-random-bank]", () => practiceQuestion(randomItem([...QUESTION_BANK.repeat, ...QUESTION_BANK.interview]).id));
  bindClick("[data-export-history]", () => exportHistory());
  bindClick("[data-clear-history]", () => clearHistoryWithConfirm());
  bindClick("[data-save-setup]", () => saveSetupFromForm());
  bindClick("[data-clear-setup]", () => clearSetupWithConfirm());

  document.querySelectorAll("[data-practice-question]").forEach((button) => {
    button.addEventListener("click", () => practiceQuestion(button.dataset.practiceQuestion));
  });

  document.querySelectorAll("[data-review-history]").forEach((button) => {
    button.addEventListener("click", () => reviewHistory(button.dataset.reviewHistory));
  });
}

function bindClick(selector, handler) {
  document.querySelectorAll(selector).forEach((element) => {
    element.addEventListener("click", handler);
  });
}

async function startRecording() {
  if (state.isRecording) return;
  clearCurrentWork(false);
  state.isRecording = true;
  state.status = SpeechRecognitionApi ? "正在录音，自动转写中" : "正在录音，可稍后手动输入转写";
  state.elapsed = 0;
  state.duration = 0;
  render();
  startTimer();

  if (navigator.mediaDevices && window.MediaRecorder) {
    try {
      state.stream = await navigator.mediaDevices.getUserMedia({ audio: true });
      state.mediaRecorder = new MediaRecorder(state.stream);
      const chunks = [];
      state.mediaRecorder.addEventListener("dataavailable", (event) => {
        if (event.data.size > 0) chunks.push(event.data);
      });
      state.mediaRecorder.addEventListener("stop", async () => {
        if (state.audioUrl) URL.revokeObjectURL(state.audioUrl);
        const blob = new Blob(chunks, { type: "audio/webm" });
        state.audioBlob = blob;
        state.audioUrl = URL.createObjectURL(blob);
        state.audioAnalysisPending = true;
        render();
        state.audioStats = await analyzeAudioBlob(blob, state.duration || state.elapsed);
        state.audioAnalysisPending = false;
        if (state.transcript.trim() && !state.feedback) {
          analyzeCurrent();
          return;
        }
        render();
      });
      state.mediaRecorder.start();
    } catch (error) {
      showToast("麦克风权限没有打开。你仍然可以手动输入转写后分析。");
    }
  }

  startRecognition();
}

function stopRecording() {
  if (!state.isRecording) return;

  state.isRecording = false;
  state.duration = state.elapsed;
  state.status = state.transcript.trim() ? "录音完成，已生成转写" : "录音完成，请检查或手动输入转写";
  stopTimer();

  if (state.mediaRecorder && state.mediaRecorder.state !== "inactive") {
    state.mediaRecorder.stop();
  }
  if (state.stream) {
    state.stream.getTracks().forEach((track) => track.stop());
  }
  if (state.recognition) {
    try {
      state.recognition.stop();
    } catch (error) {
      // The browser may already have stopped recognition.
    }
  }

  state.mediaRecorder = null;
  state.stream = null;
  state.recognition = null;
  render();

  if (state.transcript.trim()) {
    window.setTimeout(() => {
      if (!state.feedback && !state.audioAnalysisPending) analyzeCurrent();
    }, 900);
  }
}

function startRecognition() {
  if (!SpeechRecognitionApi) return;
  try {
    const recognition = new SpeechRecognitionApi();
    recognition.lang = "en-US";
    recognition.interimResults = true;
    recognition.continuous = true;
    recognition.maxAlternatives = 1;
    recognition.addEventListener("result", (event) => {
      let text = "";
      for (let index = 0; index < event.results.length; index += 1) {
        const result = event.results[index];
        const alternative = result[0];
        text += `${alternative.transcript} `;
        if (result.isFinal && index >= event.resultIndex) {
          state.recognitionStats.finalSegments += 1;
          if (Number.isFinite(alternative.confidence) && alternative.confidence > 0) {
            state.recognitionStats.confidenceSamples.push(alternative.confidence);
          }
        } else if (!result.isFinal) {
          state.recognitionStats.interimSegments += 1;
        }
      }
      state.transcript = cleanupSpacing(text);
      const input = document.querySelector("#transcriptInput");
      if (input) input.value = state.transcript;
    });
    recognition.addEventListener("error", () => {
      if (state.isRecording) {
        state.status = "识别暂时不可用，可手动输入转写";
        render();
      }
    });
    recognition.addEventListener("end", () => {
      if (state.isRecording) {
        try {
          recognition.start();
        } catch (error) {
          state.status = "识别已暂停，可继续录音后手动修改文本";
          updateStatusDom();
        }
      }
    });
    state.recognition = recognition;
    recognition.start();
  } catch (error) {
    state.status = "识别启动失败，可手动输入转写";
    updateStatusDom();
  }
}

function startTimer() {
  stopTimer();
  const startedAt = Date.now();
  state.timerId = window.setInterval(() => {
    state.elapsed = Math.floor((Date.now() - startedAt) / 1000);
    updateTimerDom();
  }, 250);
}

function stopTimer() {
  if (state.timerId) {
    window.clearInterval(state.timerId);
    state.timerId = null;
  }
}

function updateTimerDom() {
  const timer = document.querySelector("#timer");
  if (timer) timer.textContent = formatTime(state.elapsed);
}

function updateStatusDom() {
  const status = document.querySelector(".status-line span:last-child");
  if (status) status.textContent = state.status;
}

function speakPrompt() {
  const question = getActiveQuestion(state.tab === "full" && state.full.active);
  if (!window.speechSynthesis) {
    showToast("当前浏览器不支持语音播放。");
    return;
  }

  const utterance = new SpeechSynthesisUtterance(question.text);
  utterance.lang = "en-US";
  utterance.rate = question.type === "repeat" ? 0.88 : 0.94;
  utterance.pitch = 1;
  window.speechSynthesis.cancel();
  window.speechSynthesis.speak(utterance);
  state.status = question.type === "repeat" ? "正在播放句子" : "正在朗读题目";
  render();
}

async function analyzeCurrent() {
  if (state.isScoring) return;
  const question = getActiveQuestion(state.tab === "full" && state.full.active);
  const transcript = cleanupSpacing(state.transcript);
  state.transcript = transcript;

  if (!transcript) {
    showToast("还没有转写文本。可以先录音，也可以手动输入你说的内容。");
    return;
  }

  state.isScoring = true;
  state.status = "正在生成评分报告";
  render();

  const scoringContext = getScoringContext();
  const feedback = question.type === "repeat"
    ? createRepeatFeedback(question, transcript, state.duration || state.elapsed, scoringContext)
    : createInterviewFeedback(question, transcript, state.duration || state.elapsed, scoringContext);
  await enhanceFeedbackWithExternalScorers(feedback, question, transcript, state.duration || state.elapsed, scoringContext);
  feedback.comparison = buildAttemptComparison(feedback, findPreviousAttempt(question.id, transcript));

  state.feedback = feedback;
  state.isScoring = false;
  state.status = "反馈已生成，可以修改后重录";

  const signature = `${question.id}|${transcript}|${feedback.score}|${state.duration}`;
  if (state.lastSavedSignature !== signature) {
    saveHistoryItem({
      id: crypto.randomUUID ? crypto.randomUUID() : String(Date.now()),
      createdAt: new Date().toISOString(),
      type: question.type,
      questionId: question.id,
      prompt: question.text,
      transcript,
      score: feedback.score,
      duration: state.duration || state.elapsed,
      summary: feedback.summary,
      issues: feedback.issues,
      strengths: feedback.strengths,
      detailScores: feedback.detailScores,
      pronunciation: feedback.pronunciation,
      rubricProfile: feedback.rubricProfile,
      comparison: feedback.comparison
    });
    state.lastSavedSignature = signature;
  }

  if (state.full.active) {
    state.full.results[state.full.index] = {
      question,
      transcript,
      feedback
    };
  }

  render();
}

function createRepeatFeedback(question, transcript, duration, context = {}) {
  const refWords = normalizeWords(question.text);
  const hypWords = normalizeWords(transcript);
  const alignment = alignWords(refWords, hypWords);
  const stats = countAlignment(alignment);
  const refLength = Math.max(1, refWords.length);
  const hypLength = Math.max(1, hypWords.length);
  const exactRecall = stats.matches / refLength;
  const precision = stats.matches / hypLength;
  const contentRecall = getContentRecall(alignment, refWords);
  const omissionRate = stats.missing / refLength;
  const substitutionRate = stats.substitutions / refLength;
  const extraRate = stats.extras / refLength;
  const lengthFit = clamp01(1 - Math.abs(hypWords.length - refWords.length) / Math.max(refWords.length, 4));
  const sequenceScore = clamp01(1 - (stats.substitutions + stats.extras * 0.65 + stats.missing * 0.85) / refLength);
  const expectedSeconds = Math.max(3, refWords.length / 2.25);
  const paceScore = duration ? clamp01(1 - Math.abs(duration - expectedSeconds) / Math.max(expectedSeconds, 5)) : 0.58;
  const fillerPenalty = getFillerPenalty(transcript);
  const audioQualityScore = getAudioQualityScore(context.audioStats);
  const pauseScore = getPauseScore(context.audioStats, duration);
  const recognitionConfidenceScore = getRecognitionConfidenceScore(context.recognitionStats);
  const processingScore = clamp01(exactRecall * 0.68 + precision * 0.18 + contentRecall * 0.14 - extraRate * 0.16);
  const completenessScore = clamp01(1 - omissionRate * 1.35);
  const pronunciationScore = getPronunciationProxy({
    exactRecall,
    precision,
    contentRecall,
    deliveryScore: paceScore,
    audioQualityScore,
    recognitionConfidenceScore,
    fillerPenalty
  });
  const intelligibilityProxy = clamp01(precision * 0.35 + contentRecall * 0.2 + recognitionConfidenceScore * 0.2 + audioQualityScore * 0.15 + lengthFit * 0.1 - fillerPenalty);
  const deliveryScore = clamp01(paceScore * 0.48 + pauseScore * 0.22 + lengthFit * 0.16 + audioQualityScore * 0.14 - fillerPenalty);
  const composite = clamp01(
    processingScore * 0.34 +
    completenessScore * 0.2 +
    sequenceScore * 0.14 +
    pronunciationScore * 0.2 +
    deliveryScore * 0.12
  );
  const score = applyRepeatCaps(roundHalf(1 + composite * 5), {
    exactRecall,
    precision,
    hypWords,
    refWords,
    stats,
    deliveryScore
  });
  const issues = [];
  const strengths = [];

  if (processingScore >= 0.82) strengths.push("听到的核心信息处理比较准确，关键词保留较好。");
  if (completenessScore >= 0.82) strengths.push("原句覆盖较完整，没有明显只复述一小段。");
  if (sequenceScore >= 0.8) strengths.push("语序和句子骨架比较稳定，听起来更接近原句。");
  if (pronunciationScore >= 0.74) strengths.push("发音可识别度较好，系统能较稳定地捕捉到你的词。");
  if (deliveryScore >= 0.78) strengths.push("节奏接近自然复述速度，没有明显拖长或抢跑。");

  if (stats.missing > 0) issues.push(`漏掉 ${stats.missing} 个原句词，下一遍先抓主语、动词、时间和地点信息。`);
  if (stats.substitutions > 0) issues.push(`有 ${stats.substitutions} 处替换词；复述题先追求准确，不要主动改写。`);
  if (stats.extras > 2) issues.push("多说了较多原句没有的词，可能影响“准确复述”这一项。");
  if (contentRecall < 0.75) issues.push("内容词保留不够，优先听清名词、动词、形容词和数字信息。");
  if (pronunciationScore < 0.55) issues.push("发音可识别度偏低；优先放慢重读内容词，并把词尾音说完整。");
  if (audioQualityScore < 0.55) issues.push("录音质量会影响评分，建议离麦克风近一点并避开背景噪音。");
  if (duration && deliveryScore < 0.58) issues.push("节奏与原句差距较大，建议按意群复述，而不是逐词挤出来。");
  if (!issues.length) issues.push("下一遍可以挑战只听一遍，并保持同样准确度。");
  if (!strengths.length) strengths.push("你已经完成了一次可分析的复述，下一遍从减少漏词和替换词开始。");

  return {
    type: "repeat",
    score,
    summary: `专业模拟评分：${getBandLabel(score)}。听辨准确约 ${Math.round(processingScore * 100)}%，完整度约 ${Math.round(completenessScore * 100)}%。`,
    strengths,
    issues: issues.slice(0, 3),
    improvedAnswer: "下一遍按 ETS 新口语的复述目标练：先保留原句含义和关键词，再追求自然节奏。听完后用 2-3 个意群复述，避免主动换词。",
    metrics: [
      { label: "听辨处理", value: `${Math.round(processingScore * 100)}%` },
      { label: "完整复述", value: `${Math.round(completenessScore * 100)}%` },
      { label: "语序稳定", value: `${Math.round(sequenceScore * 100)}%` },
      { label: "发音可识别", value: `${Math.round(pronunciationScore * 100)}%` },
      { label: "清晰节奏", value: `${Math.round(deliveryScore * 100)}%` },
      { label: "漏/换/多", value: `${stats.missing}/${stats.substitutions}/${stats.extras}` }
    ],
    detailScores: [
      { label: "听辨处理", score: processingScore, detail: "原句词、内容词和复述文本的匹配程度。" },
      { label: "完整复述", score: completenessScore, detail: "漏词比例、句子长度和整体覆盖度。" },
      { label: "语序稳定", score: sequenceScore, detail: "替换、多说和漏说对原句结构的影响。" },
      { label: "发音可识别", score: pronunciationScore, detail: "ASR 识别稳定性、内容词匹配和录音质量的综合代理分。" },
      { label: "清晰节奏", score: deliveryScore, detail: "回答时长、停顿比例和自然复述节奏。" }
    ],
    rubricProfile: [
      { label: "复述准确", weight: 0.54, note: "听到的词和核心信息能否被准确保留下来。" },
      { label: "完整与顺序", weight: 0.22, note: "是否漏掉关键片段，句子骨架是否接近原句。" },
      { label: "清晰可懂", weight: 0.14, note: "语音识别稳定性、音频质量和内容词清晰度。" },
      { label: "自然节奏", weight: 0.1, note: "是否按意群自然复述，而不是拖长或抢跑。" }
    ],
    pronunciation: buildPronunciationReport({
      pronunciationScore,
      recognitionConfidenceScore,
      audioQualityScore,
      pauseScore,
      audioStats: context.audioStats,
      recognitionStats: context.recognitionStats,
      phonemeFocus: inferPronunciationFocus({ alignment, refWords, hypWords, transcript }),
      wordCount: hypWords.length,
      duration
    }),
    alignment
  };
}

function createInterviewFeedback(question, transcript, duration, context = {}) {
  const words = normalizeWords(transcript);
  const wordCount = words.length;
  const uniqueRatio = wordCount ? new Set(words).size / wordCount : 0;
  const wpm = duration ? Math.round(wordCount / (duration / 60)) : 0;
  const lower = ` ${transcript.toLowerCase()} `;
  const reasonMarkers = [" because ", " since ", " the reason ", " one reason ", " this is because ", " so "];
  const exampleMarkers = [" for example ", " for instance ", " such as ", " last semester ", " once ", " when i ", " in my class ", " in one course "];
  const structureMarkers = [" first ", " also ", " however ", " in addition ", " on the other hand ", " overall ", " in the end ", " that is why "];
  const resultMarkers = [" as a result ", " this helps ", " this made ", " therefore ", " so that ", " because of this "];
  const directMarkers = [" i think ", " i believe ", " in my opinion ", " i prefer ", " i would ", " yes ", " no ", " one ", " my "];
  const fillerMatches = lower.match(/\b(um|uh|er|like|you know|sort of|kind of)\b/g) || [];
  const repeatedWordPenalty = getRepeatedWordPenalty(words);
  const hasReason = hasAnyMarker(lower, reasonMarkers);
  const hasExample = hasAnyMarker(lower, exampleMarkers);
  const hasStructure = hasAnyMarker(lower, structureMarkers);
  const hasResult = hasAnyMarker(lower, resultMarkers);
  const hasDirectAnswer = hasAnyMarker(lower.slice(0, 120), directMarkers) || /^[\s"']*(yes|no)\b/i.test(transcript);
  const promptKeywords = getContentWords(normalizeWords(question.text));
  const promptOverlap = promptKeywords.length ? countOverlap(promptKeywords, new Set(words)) / promptKeywords.length : 0.45;
  const relevanceScore = clamp01(promptOverlap * 1.6 + (wordCount >= 28 ? 0.25 : 0));
  const lengthScore = getInterviewLengthScore(wordCount);
  const demandScore = getPromptDemandScore(question.text, { hasReason, hasExample, hasDirectAnswer, lower });
  const taskScore = clamp01(lengthScore * 0.32 + relevanceScore * 0.22 + demandScore * 0.26 + (hasDirectAnswer ? 0.2 : 0));
  const developmentScore = clamp01(
    (hasReason ? 0.28 : 0) +
    (hasExample ? 0.34 : 0) +
    (hasResult ? 0.18 : 0) +
    getSpecificDetailScore(transcript, words) * 0.2
  );
  const organizationScore = clamp01(
    (hasDirectAnswer ? 0.25 : 0) +
    Math.min(0.35, countMarkers(lower, structureMarkers) * 0.12 + countMarkers(lower, reasonMarkers) * 0.08) +
    (hasExample ? 0.18 : 0) +
    (hasResult ? 0.12 : 0) +
    getAnswerShapeScore(words, lower) * 0.1
  );
  const lexicalScore = clamp01((uniqueRatio * 1.45 + getContentDensity(words) * 0.55) / 1.35);
  const sentenceComplexityScore = clamp01(countMarkers(lower, [" because ", " when ", " if ", " although ", " while ", " which ", " that "]) / 3);
  const languageScore = clamp01(lexicalScore * 0.55 + sentenceComplexityScore * 0.2 + lengthScore * 0.2 - repeatedWordPenalty);
  const paceScore = wpm ? clamp01(1 - Math.abs(wpm - 125) / 85) : 0.56;
  const fillerPenalty = Math.min(0.25, fillerMatches.length * 0.05);
  const durationFit = duration ? getDurationFit(duration, 35, 75) : 0.58;
  const audioQualityScore = getAudioQualityScore(context.audioStats);
  const pauseScore = getPauseScore(context.audioStats, duration);
  const recognitionConfidenceScore = getRecognitionConfidenceScore(context.recognitionStats);
  const pronunciationScore = getPronunciationProxy({
    exactRecall: relevanceScore,
    precision: lexicalScore,
    contentRecall: developmentScore,
    deliveryScore: paceScore,
    audioQualityScore,
    recognitionConfidenceScore,
    fillerPenalty
  });
  const deliveryScore = clamp01(paceScore * 0.34 + durationFit * 0.18 + pauseScore * 0.2 + pronunciationScore * 0.18 + audioQualityScore * 0.1 - fillerPenalty);
  const composite = clamp01(
    taskScore * 0.27 +
    developmentScore * 0.27 +
    organizationScore * 0.2 +
    languageScore * 0.16 +
    deliveryScore * 0.1
  );
  const score = applyInterviewCaps(roundHalf(clamp(1, 6, 1 + composite * 5)), {
    wordCount,
    hasDirectAnswer,
    hasReason,
    hasExample,
    taskScore,
    developmentScore,
    deliveryScore,
    wpm
  });
  const strengths = [];
  const issues = [];

  if (taskScore >= 0.72) strengths.push("回答能回应题目要求，开头观点比较明确。");
  if (developmentScore >= 0.72) strengths.push("有理由和具体例子，内容展开比单句结论更充分。");
  if (organizationScore >= 0.7) strengths.push("答案有一定组织，听者能跟上观点推进。");
  if (languageScore >= 0.68) strengths.push("词汇和句式有一定变化，表达不只是重复简单词。");
  if (pronunciationScore >= 0.72) strengths.push("发音可识别度较好，关键词更容易被稳定听清。");
  if (deliveryScore >= 0.72) strengths.push("语速和停顿大致自然，适合口语面试回答。");

  if (wordCount < 35) issues.push("回答偏短，建议扩展到 45-75 词，至少包含一个理由和一个例子。");
  if (!hasDirectAnswer) issues.push("开头没有直接回应题目，建议第一句就给出选择、观点或对象。");
  if (!hasReason) issues.push("缺少明确理由，可以加入 because 或 the main reason is。");
  if (!hasExample) issues.push("缺少个人经历或课堂例子，建议用 for example 或 once 展开。");
  if (!hasResult) issues.push("例子后缺少结果或意义，可以补一句 this helped me 或 as a result。");
  if (organizationScore < 0.52) issues.push("组织感偏弱，建议使用“观点-理由-例子-结果”的顺序。");
  if (wpm && wpm < 85) issues.push("语速偏慢，下一遍先准备关键词，再连续说完整句。");
  if (wpm > 170) issues.push("语速偏快，容易牺牲清晰度，建议每个意群后自然停顿。");
  if (fillerMatches.length >= 3) issues.push("填充词较多，可以用短暂停顿代替 um、uh、like。");
  if (pronunciationScore < 0.55) issues.push("发音可识别度偏低，建议放慢内容词并加强词尾音。");
  if (audioQualityScore < 0.55) issues.push("录音质量会影响口语判断，建议减少背景噪音并保持稳定音量。");
  if (repeatedWordPenalty > 0.08) issues.push("重复词偏多，试着替换一两个高频动词或形容词。");

  if (!strengths.length) strengths.push("你已经给出可分析的回答，下一遍先补足观点、理由和例子。");
  if (!issues.length) issues.push("下一遍可以压缩开头，把更多时间留给具体例子和结果。");

  return {
    type: "interview",
    score,
    summary: `专业模拟评分：${getBandLabel(score)}。回答约 ${wordCount} 词，语速约 ${wpm || "-"} WPM。`,
    strengths: strengths.slice(0, 3),
    issues: issues.slice(0, 3),
    improvedAnswer: question.sample,
    metrics: [
      { label: "任务回应", value: `${Math.round(taskScore * 100)}%` },
      { label: "观点展开", value: `${Math.round(developmentScore * 100)}%` },
      { label: "组织逻辑", value: `${Math.round(organizationScore * 100)}%` },
      { label: "语言使用", value: `${Math.round(languageScore * 100)}%` },
      { label: "发音可识别", value: `${Math.round(pronunciationScore * 100)}%` },
      { label: "表达流利", value: `${Math.round(deliveryScore * 100)}%` }
    ],
    detailScores: [
      { label: "任务回应", score: taskScore, detail: "是否直接回应题目，并围绕题目关键词展开。" },
      { label: "观点展开", score: developmentScore, detail: "是否给出理由、例子、结果或意义说明。" },
      { label: "组织逻辑", score: organizationScore, detail: "是否形成清楚的观点、理由、例子、结果顺序。" },
      { label: "语言使用", score: languageScore, detail: "词汇多样性、内容词密度和复合句使用情况。" },
      { label: "发音可识别", score: pronunciationScore, detail: "识别置信度、音频质量和关键词清晰度的综合代理分。" },
      { label: "表达流利", score: deliveryScore, detail: "语速、时长、停顿、填充词和音频稳定性。" }
    ],
    rubricProfile: [
      { label: "任务与展开", weight: 0.54, note: "是否直接回应题目，并用理由和例子充分展开。" },
      { label: "组织逻辑", weight: 0.2, note: "观点、理由、例子、结果是否形成清晰顺序。" },
      { label: "语言使用", weight: 0.16, note: "词汇、句式、语法和表达自然度。" },
      { label: "清晰流利", weight: 0.1, note: "发音可识别、语速、停顿和音频稳定性。" }
    ],
    pronunciation: buildPronunciationReport({
      pronunciationScore,
      recognitionConfidenceScore,
      audioQualityScore,
      pauseScore,
      audioStats: context.audioStats,
      recognitionStats: context.recognitionStats,
      phonemeFocus: inferPronunciationFocus({ alignment: [], refWords: promptKeywords, hypWords: words, transcript }),
      wordCount,
      duration
    })
  };
}

function getBandLabel(score) {
  if (score >= 5.5) return "高级水平";
  if (score >= 4.5) return "良好水平";
  if (score >= 3.5) return "中等水平";
  if (score >= 2.5) return "基础水平";
  return "需要大量练习";
}

async function enhanceFeedbackWithExternalScorers(feedback, question, transcript, duration, context) {
  const setup = state.setup || getDefaultSetupConfig();
  const errors = [];

  if (setup.ai.enabled) {
    state.status = "正在调用 AI 内容评分";
    render();
    try {
      const aiResult = await callAiScorer(setup.ai, question, transcript, duration, feedback);
      mergeAiFeedback(feedback, aiResult);
    } catch (error) {
      errors.push(`AI评分失败：${error.message}`);
    }
  }

  if (setup.pronunciation.enabled) {
    state.status = "正在调用外部发音评分";
    render();
    try {
      const pronunciationResult = await callPronunciationScorer(setup.pronunciation, question, transcript, duration, context, state.audioBlob);
      mergeExternalPronunciation(feedback, pronunciationResult, question.type);
    } catch (error) {
      errors.push(`发音评分失败：${error.message}`);
    }
  }

  if (errors.length) {
    feedback.externalErrors = errors;
    feedback.issues = [...feedback.issues, ...errors].slice(0, 4);
  }
}

async function callAiScorer(config, question, transcript, duration, feedback) {
  if (!config.endpoint || !config.apiKey || !config.model) {
    throw new Error("请在 Setup 里填写 AI endpoint、API key 和 model。");
  }

  const systemPrompt = [
    "You are a TOEFL iBT 2026 speaking evaluator.",
    "Score on a 1-6 half-point scale.",
    "Return only valid JSON with keys: score, summary, strengths, issues, improvedAnswer, grammarNotes, dimensionScores.",
    "dimensionScores should contain percentages from 0 to 100."
  ].join(" ");
  const userPrompt = JSON.stringify({
    taskType: question.type,
    prompt: question.text,
    transcript,
    durationSeconds: duration,
    localScore: feedback.score,
    localSummary: feedback.summary,
    expectedRubric: question.type === "repeat"
      ? ["accuracy of repetition", "completeness", "word order", "clear intelligibility", "natural pace"]
      : ["task response", "development", "organization", "language use", "clear intelligibility", "fluency"]
  }, null, 2);

  const headers = {
    "Content-Type": "application/json",
    Authorization: `Bearer ${config.apiKey}`
  };
  const body = config.mode === "chat"
    ? {
        model: config.model,
        temperature: clamp(0, 1, Number(config.temperature) || 0),
        response_format: { type: "json_object" },
        messages: [
          { role: "system", content: systemPrompt },
          { role: "user", content: userPrompt }
        ]
      }
    : {
        model: config.model,
        temperature: clamp(0, 1, Number(config.temperature) || 0),
        instructions: systemPrompt,
        input: userPrompt
      };

  const response = await fetch(config.endpoint, {
    method: "POST",
    headers,
    body: JSON.stringify(body)
  });
  if (!response.ok) {
    const text = await response.text();
    throw new Error(`${response.status} ${text.slice(0, 160)}`);
  }

  const data = await response.json();
  const text = extractAiText(data);
  return parseJsonLike(text);
}

function extractAiText(data) {
  if (typeof data.output_text === "string") return data.output_text;
  if (data.choices && data.choices[0] && data.choices[0].message) return data.choices[0].message.content || "";
  if (data.output && data.output[0] && data.output[0].content) {
    const content = data.output.flatMap((item) => item.content || []);
    const textItem = content.find((item) => item.text || item.type === "output_text");
    return textItem ? (textItem.text || "") : "";
  }
  return JSON.stringify(data);
}

function parseJsonLike(text) {
  try {
    return JSON.parse(text);
  } catch (error) {
    const match = String(text).match(/\{[\s\S]*\}/);
    if (match) return JSON.parse(match[0]);
    throw new Error("AI 返回的不是可解析 JSON。");
  }
}

function mergeAiFeedback(feedback, aiResult) {
  const aiScore = clamp(1, 6, Number(aiResult.score));
  feedback.externalAi = {
    score: Number.isFinite(aiScore) ? roundHalf(aiScore) : null,
    summary: cleanupSpacing(aiResult.summary || ""),
    strengths: Array.isArray(aiResult.strengths) ? aiResult.strengths.slice(0, 4) : [],
    issues: Array.isArray(aiResult.issues) ? aiResult.issues.slice(0, 4) : [],
    grammarNotes: Array.isArray(aiResult.grammarNotes) ? aiResult.grammarNotes.slice(0, 6) : [],
    dimensionScores: aiResult.dimensionScores || {}
  };

  if (feedback.externalAi.score) {
    feedback.localScore = feedback.localScore || feedback.score;
    feedback.score = roundHalf(feedback.localScore * 0.65 + feedback.externalAi.score * 0.35);
    feedback.metrics.push({ label: "AI评分", value: `${feedback.externalAi.score}/6` });
    feedback.summary = `${feedback.summary} AI增强后总分 ${feedback.score}/6。`;
  }
  if (cleanupSpacing(aiResult.improvedAnswer || "")) feedback.improvedAnswer = aiResult.improvedAnswer;
}

async function callPronunciationScorer(config, question, transcript, duration, context, audioBlob) {
  if (!config.apiKey) throw new Error("请在 Setup 里填写发音评分 API key。");
  if (!audioBlob) throw new Error("没有可发送的录音，请先录音再分析。");

  if (config.provider === "custom") {
    if (!config.endpoint) throw new Error("Custom provider 需要 endpoint URL。");
    return callCustomPronunciationEndpoint(config, question, transcript, duration, context, audioBlob);
  }

  const region = config.azureRegion;
  const endpoint = config.endpoint || (region
    ? `https://${region}.stt.speech.microsoft.com/speech/recognition/conversation/cognitiveservices/v1?language=en-US&format=detailed`
    : "");
  if (!endpoint) throw new Error("Azure provider 需要 region 或 endpoint override。");

  const wavBlob = await convertBlobToWav16k(audioBlob);
  const referenceText = question.type === "repeat" ? question.text : transcript;
  const assessment = {
    ReferenceText: referenceText.slice(0, 5000),
    GradingSystem: "HundredMark",
    Granularity: "Phoneme",
    Dimension: "Comprehensive",
    EnableMiscue: true
  };
  const response = await fetch(endpoint, {
    method: "POST",
    headers: {
      "Ocp-Apim-Subscription-Key": config.apiKey,
      "Pronunciation-Assessment": base64EncodeUnicode(JSON.stringify(assessment)),
      "Content-Type": "audio/wav; codecs=audio/pcm; samplerate=16000",
      Accept: "application/json"
    },
    body: wavBlob
  });
  if (!response.ok) {
    const text = await response.text();
    throw new Error(`${response.status} ${text.slice(0, 160)}`);
  }
  const data = await response.json();
  return normalizeAzurePronunciation(data);
}

async function callCustomPronunciationEndpoint(config, question, transcript, duration, context, audioBlob) {
  const audioBase64 = await blobToBase64(audioBlob);
  const response = await fetch(config.endpoint, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      ...(config.apiKey ? { Authorization: `Bearer ${config.apiKey}` } : {})
    },
    body: JSON.stringify({
      taskType: question.type,
      prompt: question.text,
      referenceText: question.type === "repeat" ? question.text : transcript,
      transcript,
      duration,
      audio: audioBase64,
      audioStats: context.audioStats
    })
  });
  if (!response.ok) {
    const text = await response.text();
    throw new Error(`${response.status} ${text.slice(0, 160)}`);
  }
  return normalizeCustomPronunciation(await response.json());
}

function normalizeAzurePronunciation(data) {
  const best = data.NBest && data.NBest[0] ? data.NBest[0] : {};
  const assessment = best.PronunciationAssessment || data.PronunciationAssessment || {};
  const words = best.Words || data.Words || [];
  const phonemes = [];
  words.forEach((word) => {
    (word.Phonemes || []).forEach((phoneme) => {
      const score = phoneme.PronunciationAssessment && Number(phoneme.PronunciationAssessment.AccuracyScore);
      if (Number.isFinite(score) && score < 75) {
        phonemes.push({
          sound: phoneme.Phoneme || "",
          word: word.Word || "",
          score,
          errorType: phoneme.PronunciationAssessment.ErrorType || ""
        });
      }
    });
  });
  return {
    provider: "Azure Speech",
    raw: data,
    score: normalizeHundredScore(assessment.PronScore),
    accuracy: normalizeHundredScore(assessment.AccuracyScore),
    fluency: normalizeHundredScore(assessment.FluencyScore),
    completeness: normalizeHundredScore(assessment.CompletenessScore),
    prosody: normalizeHundredScore(assessment.ProsodyScore),
    words: words.slice(0, 80).map((word) => ({
      word: word.Word,
      score: word.PronunciationAssessment ? word.PronunciationAssessment.AccuracyScore : null,
      errorType: word.PronunciationAssessment ? word.PronunciationAssessment.ErrorType : ""
    })),
    phonemes: phonemes.slice(0, 12)
  };
}

function normalizeCustomPronunciation(data) {
  return {
    provider: data.provider || "Custom",
    raw: data,
    score: normalizeHundredScore(data.score || data.pronunciationScore),
    accuracy: normalizeHundredScore(data.accuracy),
    fluency: normalizeHundredScore(data.fluency),
    completeness: normalizeHundredScore(data.completeness),
    prosody: normalizeHundredScore(data.prosody),
    words: Array.isArray(data.words) ? data.words.slice(0, 80) : [],
    phonemes: Array.isArray(data.phonemes) ? data.phonemes.slice(0, 12) : []
  };
}

function normalizeHundredScore(value) {
  const number = Number(value);
  if (!Number.isFinite(number)) return null;
  return number > 1 ? clamp01(number / 100) : clamp01(number);
}

function mergeExternalPronunciation(feedback, pronunciationResult, taskType) {
  if (!pronunciationResult) return;
  feedback.externalPronunciation = pronunciationResult;
  if (feedback.pronunciation) {
    feedback.pronunciation.external = pronunciationResult;
    if (Number.isFinite(pronunciationResult.score)) {
      feedback.pronunciation.score = clamp01(feedback.pronunciation.score * 0.5 + pronunciationResult.score * 0.5);
      feedback.metrics.push({ label: "外部发音", value: `${Math.round(pronunciationResult.score * 100)}%` });
    }
    if (pronunciationResult.phonemes && pronunciationResult.phonemes.length) {
      const externalFocus = pronunciationResult.phonemes.map((item) => ({
        sound: item.sound || item.phoneme || "",
        label: `${item.word || ""} ${Number.isFinite(Number(item.score)) ? Math.round(Number(item.score)) : ""}`.trim(),
        tip: item.errorType ? `外部评测标记：${item.errorType}` : "外部评测认为这个音素需要重点练习。",
        words: [item.word || ""].filter(Boolean)
      }));
      feedback.pronunciation.phonemeFocus = [...externalFocus, ...(feedback.pronunciation.phonemeFocus || [])].slice(0, 8);
    }
  }

  if (Number.isFinite(pronunciationResult.score)) {
    feedback.localScore = feedback.localScore || feedback.score;
    const externalScore = 1 + pronunciationResult.score * 5;
    const weight = taskType === "repeat" ? 0.15 : 0.1;
    feedback.score = roundHalf(feedback.score * (1 - weight) + externalScore * weight);
  }
}

function getScoringContext() {
  return {
    audioStats: state.audioStats,
    recognitionStats: {
      confidenceSamples: [...state.recognitionStats.confidenceSamples],
      finalSegments: state.recognitionStats.finalSegments,
      interimSegments: state.recognitionStats.interimSegments
    }
  };
}

async function analyzeAudioBlob(blob, fallbackDuration) {
  const AudioContextApi = window.AudioContext || window.webkitAudioContext;
  if (!blob || !AudioContextApi) return createAudioStatsFallback(fallbackDuration, "Web Audio is not available.");

  let audioContext;
  try {
    const arrayBuffer = await blob.arrayBuffer();
    audioContext = new AudioContextApi();
    const audioBuffer = await audioContext.decodeAudioData(arrayBuffer.slice(0));
    const sampleRate = audioBuffer.sampleRate;
    const sampleCount = audioBuffer.length;
    const duration = audioBuffer.duration || fallbackDuration || 0;
    const frameSize = Math.max(256, Math.floor(sampleRate * 0.02));
    const frames = [];
    let sumSquares = 0;
    let peak = 0;
    let clipping = 0;

    for (let start = 0; start < sampleCount; start += frameSize) {
      let frameSquares = 0;
      let frameSamples = 0;
      for (let offset = 0; offset < frameSize && start + offset < sampleCount; offset += 1) {
        let mixed = 0;
        for (let channel = 0; channel < audioBuffer.numberOfChannels; channel += 1) {
          mixed += audioBuffer.getChannelData(channel)[start + offset] || 0;
        }
        const sample = mixed / Math.max(1, audioBuffer.numberOfChannels);
        const abs = Math.abs(sample);
        if (abs > peak) peak = abs;
        if (abs > 0.985) clipping += 1;
        frameSquares += sample * sample;
        sumSquares += sample * sample;
        frameSamples += 1;
      }
      frames.push(Math.sqrt(frameSquares / Math.max(1, frameSamples)));
    }

    const rms = Math.sqrt(sumSquares / Math.max(1, sampleCount));
    const noiseFloor = percentile(frames, 0.18);
    const speechFloor = percentile(frames, 0.75);
    const silenceThreshold = clamp(0.006, 0.05, Math.max(noiseFloor * 2.35, rms * 0.3));
    const frameDuration = frameSize / sampleRate;
    let silenceFrames = 0;
    let currentSilence = 0;
    let longestSilence = 0;
    let pauseCount = 0;
    let speechSquares = 0;
    let speechFrameCount = 0;
    let noiseSquares = 0;
    let noiseFrameCount = 0;

    frames.forEach((frameRms) => {
      if (frameRms <= silenceThreshold) {
        silenceFrames += 1;
        currentSilence += frameDuration;
        noiseSquares += frameRms * frameRms;
        noiseFrameCount += 1;
      } else {
        if (currentSilence >= 0.28) pauseCount += 1;
        if (currentSilence > longestSilence) longestSilence = currentSilence;
        currentSilence = 0;
        speechSquares += frameRms * frameRms;
        speechFrameCount += 1;
      }
    });
    if (currentSilence >= 0.28) pauseCount += 1;
    if (currentSilence > longestSilence) longestSilence = currentSilence;

    const silenceSeconds = silenceFrames * frameDuration;
    const speechSeconds = Math.max(0, duration - silenceSeconds);
    const silenceRatio = duration ? clamp01(silenceSeconds / duration) : 0;
    const speechRms = Math.sqrt(speechSquares / Math.max(1, speechFrameCount));
    const noiseRms = Math.sqrt(noiseSquares / Math.max(1, noiseFrameCount));
    const snrDb = 20 * Math.log10((speechRms + 0.00001) / (noiseRms + 0.00001));

    return {
      available: true,
      duration,
      rms,
      peak,
      clippingPercent: clipping / Math.max(1, sampleCount),
      silenceRatio,
      longestSilence,
      pauseCount,
      speechSeconds,
      snrDb,
      noiseFloor,
      speechFloor
    };
  } catch (error) {
    return createAudioStatsFallback(fallbackDuration, error.message || "Audio analysis failed.");
  } finally {
    if (audioContext && audioContext.close) {
      try {
        await audioContext.close();
      } catch (error) {
        // Some browsers keep the context open for a short time after decoding.
      }
    }
  }
}

function createAudioStatsFallback(duration, reason) {
  return {
    available: false,
    duration: duration || 0,
    rms: 0,
    peak: 0,
    clippingPercent: 0,
    silenceRatio: 0,
    longestSilence: 0,
    pauseCount: 0,
    speechSeconds: duration || 0,
    snrDb: 0,
    reason
  };
}

async function convertBlobToWav16k(blob) {
  const AudioContextApi = window.AudioContext || window.webkitAudioContext;
  if (!AudioContextApi) throw new Error("当前浏览器不支持 Web Audio，无法转换 WAV。");
  let audioContext;
  try {
    const arrayBuffer = await blob.arrayBuffer();
    audioContext = new AudioContextApi();
    const audioBuffer = await audioContext.decodeAudioData(arrayBuffer.slice(0));
    const mono = mixToMono(audioBuffer);
    const resampled = resampleLinear(mono, audioBuffer.sampleRate, 16000);
    const wavBuffer = encodeWav16Bit(resampled, 16000);
    return new Blob([wavBuffer], { type: "audio/wav" });
  } finally {
    if (audioContext && audioContext.close) {
      try {
        await audioContext.close();
      } catch (error) {
        // Closing can fail briefly while the browser releases the decoder.
      }
    }
  }
}

function mixToMono(audioBuffer) {
  const output = new Float32Array(audioBuffer.length);
  for (let channel = 0; channel < audioBuffer.numberOfChannels; channel += 1) {
    const data = audioBuffer.getChannelData(channel);
    for (let index = 0; index < data.length; index += 1) {
      output[index] += data[index] / audioBuffer.numberOfChannels;
    }
  }
  return output;
}

function resampleLinear(samples, fromRate, toRate) {
  if (fromRate === toRate) return samples;
  const ratio = fromRate / toRate;
  const newLength = Math.max(1, Math.round(samples.length / ratio));
  const output = new Float32Array(newLength);
  for (let index = 0; index < newLength; index += 1) {
    const sourceIndex = index * ratio;
    const left = Math.floor(sourceIndex);
    const right = Math.min(left + 1, samples.length - 1);
    const fraction = sourceIndex - left;
    output[index] = samples[left] * (1 - fraction) + samples[right] * fraction;
  }
  return output;
}

function encodeWav16Bit(samples, sampleRate) {
  const buffer = new ArrayBuffer(44 + samples.length * 2);
  const view = new DataView(buffer);
  writeAscii(view, 0, "RIFF");
  view.setUint32(4, 36 + samples.length * 2, true);
  writeAscii(view, 8, "WAVE");
  writeAscii(view, 12, "fmt ");
  view.setUint32(16, 16, true);
  view.setUint16(20, 1, true);
  view.setUint16(22, 1, true);
  view.setUint32(24, sampleRate, true);
  view.setUint32(28, sampleRate * 2, true);
  view.setUint16(32, 2, true);
  view.setUint16(34, 16, true);
  writeAscii(view, 36, "data");
  view.setUint32(40, samples.length * 2, true);
  let offset = 44;
  for (let index = 0; index < samples.length; index += 1) {
    const value = clamp(-1, 1, samples[index]);
    view.setInt16(offset, value < 0 ? value * 0x8000 : value * 0x7fff, true);
    offset += 2;
  }
  return buffer;
}

function writeAscii(view, offset, text) {
  for (let index = 0; index < text.length; index += 1) {
    view.setUint8(offset + index, text.charCodeAt(index));
  }
}

function blobToBase64(blob) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => resolve(String(reader.result || ""));
    reader.onerror = () => reject(reader.error || new Error("Blob 转 base64 失败。"));
    reader.readAsDataURL(blob);
  });
}

function base64EncodeUnicode(text) {
  const bytes = new TextEncoder().encode(text);
  let binary = "";
  bytes.forEach((byte) => {
    binary += String.fromCharCode(byte);
  });
  return btoa(binary);
}

function getAudioQualityScore(audioStats) {
  if (!audioStats || !audioStats.available) return 0.68;
  const volumeScore = audioStats.rms < 0.012
    ? clamp01(audioStats.rms / 0.012)
    : audioStats.rms > 0.18
      ? clamp01(1 - (audioStats.rms - 0.18) / 0.2)
      : 1;
  const peakScore = audioStats.peak > 0.99 ? 0.55 : audioStats.peak > 0.92 ? 0.78 : 1;
  const clippingScore = clamp01(1 - audioStats.clippingPercent * 18);
  const snrScore = clamp01((audioStats.snrDb - 8) / 18);
  const silenceBalance = clamp01(1 - Math.abs(audioStats.silenceRatio - 0.22) / 0.55);
  return clamp01(volumeScore * 0.28 + peakScore * 0.12 + clippingScore * 0.18 + snrScore * 0.3 + silenceBalance * 0.12);
}

function getPauseScore(audioStats, duration) {
  if (!audioStats || !audioStats.available || !duration) return 0.66;
  const silenceScore = audioStats.silenceRatio <= 0.38 ? 1 : clamp01(1 - (audioStats.silenceRatio - 0.38) / 0.35);
  const longPauseScore = audioStats.longestSilence <= 1.15 ? 1 : clamp01(1 - (audioStats.longestSilence - 1.15) / 2.2);
  const pauseRate = audioStats.pauseCount / Math.max(duration / 60, 0.2);
  const pauseRateScore = pauseRate <= 11 ? 1 : clamp01(1 - (pauseRate - 11) / 18);
  return clamp01(silenceScore * 0.42 + longPauseScore * 0.34 + pauseRateScore * 0.24);
}

function getRecognitionConfidenceScore(recognitionStats) {
  const samples = recognitionStats && recognitionStats.confidenceSamples ? recognitionStats.confidenceSamples : [];
  if (!samples.length) return 0.68;
  const average = samples.reduce((sum, value) => sum + value, 0) / samples.length;
  return clamp01((average - 0.35) / 0.55);
}

function getPronunciationProxy(parts) {
  return clamp01(
    parts.exactRecall * 0.22 +
    parts.precision * 0.2 +
    parts.contentRecall * 0.15 +
    parts.recognitionConfidenceScore * 0.18 +
    parts.audioQualityScore * 0.14 +
    parts.deliveryScore * 0.11 -
    parts.fillerPenalty
  );
}

function buildPronunciationReport(data) {
  const audioStats = data.audioStats || createAudioStatsFallback(data.duration, "No audio stats.");
  const articulationRate = audioStats.available && audioStats.speechSeconds
    ? Math.round(data.wordCount / Math.max(audioStats.speechSeconds / 60, 0.1))
    : 0;
  const notes = [];

  if (!audioStats.available) notes.push("当前报告未拿到完整音频特征，发音分主要依赖转写文本和语速。");
  if (data.recognitionConfidenceScore < 0.58) notes.push("语音识别置信度偏低，可能存在发音不清、音量不足或背景噪音。");
  if (data.audioQualityScore < 0.58) notes.push("录音质量偏低，会影响发音判断，建议使用安静环境和稳定麦克风距离。");
  if (data.pauseScore < 0.58) notes.push("停顿偏多或长停顿明显，建议按意群连续说完。");
  if (audioStats.available && audioStats.clippingPercent > 0.01) notes.push("录音出现削波，音量可能过大，建议降低输入音量。");
  if (audioStats.available && audioStats.rms < 0.012) notes.push("整体音量偏低，建议靠近麦克风或提高输入音量。");
  if (!notes.length) notes.push("本地发音诊断未发现明显录音或可识别度问题。");

  return {
    mode: "Local pronunciation proxy",
    score: data.pronunciationScore,
    confidence: data.recognitionConfidenceScore,
    audioQuality: data.audioQualityScore,
    pauseControl: data.pauseScore,
    articulationRate,
    audioStats,
    notes,
    phonemeFocus: data.phonemeFocus || []
  };
}

function inferPronunciationFocus({ alignment = [], refWords = [], hypWords = [], transcript = "" }) {
  const candidates = [];
  const mismatchWords = alignment.length
    ? alignment
        .filter((item) => item.type === "missing" || item.type === "substitute")
        .flatMap((item) => [item.ref, item.hyp])
        .filter(Boolean)
    : [];
  const sourceWords = [...mismatchWords, ...getContentWords(refWords), ...getContentWords(hypWords), ...normalizeWords(transcript)];
  const uniqueWords = [...new Set(sourceWords)].filter((word) => word.length > 2);
  const rules = [
    { key: "th-voiceless", sound: "/θ/", label: "think / three / method", pattern: /(^th|th$|th)/, tip: "舌尖轻触上齿背，气流出去，不要说成 /s/ 或 /t/。" },
    { key: "th-voiced", sound: "/ð/", label: "this / those / rather", pattern: /(the|this|that|these|those|there|rather|another)/, tip: "保持舌尖轻触上齿背并带声带振动，不要说成 /d/。" },
    { key: "r-l", sound: "/r/ vs /l/", label: "right / library / clearly", pattern: /[rl]/, tip: "/r/ 舌尖不要碰上齿龈，/l/ 舌尖需要轻触上齿龈。" },
    { key: "v-w", sound: "/v/ vs /w/", label: "very / review / available", pattern: /v|w/, tip: "/v/ 用上齿轻触下唇，/w/ 是圆唇滑音，不要混在一起。" },
    { key: "final-consonant", sound: "final consonants", label: "asked / helps / project", pattern: /(ed|s|t|d|k|p|g|m|n)$/, tip: "词尾音要短而清楚，尤其是 -s、-ed、/t/、/d/。" },
    { key: "consonant-cluster", sound: "clusters", label: "students / strengths / projects", pattern: /(str|spr|skr|sts|nts|cts|ghts|rts|lds|lps|mps)/, tip: "辅音连缀不要吞音，可以先慢速拆开再连起来。" },
    { key: "long-vowel", sound: "long vowels", label: "leave / reach / improve", pattern: /(ee|ea|oo|ou|oa|ai)/, tip: "长元音需要拉开一点，但不要把一个词拖得过长。" }
  ];

  rules.forEach((rule) => {
    const words = uniqueWords.filter((word) => rule.pattern.test(word)).slice(0, 5);
    if (words.length) {
      candidates.push({
        sound: rule.sound,
        label: rule.label,
        tip: rule.tip,
        words
      });
    }
  });

  return candidates.slice(0, 4);
}

function applyRepeatCaps(score, data) {
  let capped = score;
  if (data.hypWords.length < Math.ceil(data.refWords.length * 0.45)) capped = Math.min(capped, 2.5);
  if (data.exactRecall < 0.5) capped = Math.min(capped, 3);
  if (data.exactRecall < 0.68 || data.precision < 0.62) capped = Math.min(capped, 4);
  if (data.stats.missing >= 3 || data.stats.substitutions >= 3) capped = Math.min(capped, 4.5);
  if (data.stats.extras >= 5) capped = Math.min(capped, 4.5);
  if (data.exactRecall >= 0.94 && data.precision >= 0.94 && data.deliveryScore >= 0.7) capped = Math.max(capped, 5.5);
  return roundHalf(clamp(1, 6, capped));
}

function applyInterviewCaps(score, data) {
  let capped = score;
  if (data.wordCount < 12) capped = Math.min(capped, 2);
  if (data.wordCount < 25) capped = Math.min(capped, 3);
  if (data.wordCount < 38) capped = Math.min(capped, 4);
  if (!data.hasDirectAnswer) capped = Math.min(capped, 4.5);
  if (!data.hasReason && !data.hasExample) capped = Math.min(capped, 4);
  if (!data.hasExample && data.developmentScore < 0.55) capped = Math.min(capped, 4.5);
  if ((data.wpm && (data.wpm < 70 || data.wpm > 185)) || data.deliveryScore < 0.42) capped = Math.min(capped, 5);
  if (data.wordCount >= 50 && data.hasDirectAnswer && data.hasReason && data.hasExample && data.taskScore >= 0.72 && data.developmentScore >= 0.72) {
    capped = Math.max(capped, 4.5);
  }
  return roundHalf(clamp(1, 6, capped));
}

function findPreviousAttempt(questionId, transcript) {
  const currentFingerprint = fingerprintText(transcript);
  return loadHistory().find((item) => item.questionId === questionId && fingerprintText(item.transcript) !== currentFingerprint) || null;
}

function buildAttemptComparison(feedback, previousAttempt) {
  if (!previousAttempt) return null;
  const previousScore = Number(previousAttempt.score) || 0;
  const delta = roundHalf(feedback.score - previousScore);
  const currentDetails = indexDetailScores(feedback.detailScores || []);
  const previousDetails = indexDetailScores(previousAttempt.detailScores || []);
  const notes = [];

  Object.keys(currentDetails).forEach((label) => {
    if (!Number.isFinite(previousDetails[label])) return;
    const diff = Math.round((currentDetails[label] - previousDetails[label]) * 100);
    if (Math.abs(diff) >= 6) {
      notes.push(`${label}${diff > 0 ? "提升" : "下降"} ${Math.abs(diff)}%。`);
    }
  });

  const currentPron = feedback.pronunciation ? feedback.pronunciation.score : null;
  const previousPron = previousAttempt.pronunciation ? previousAttempt.pronunciation.score : null;
  if (Number.isFinite(currentPron) && Number.isFinite(previousPron)) {
    const pronDiff = Math.round((currentPron - previousPron) * 100);
    if (Math.abs(pronDiff) >= 6) notes.push(`发音可识别度${pronDiff > 0 ? "提升" : "下降"} ${Math.abs(pronDiff)}%。`);
  }

  if (!notes.length) notes.push("这次表现与上次接近，下一遍建议只盯一个最弱维度重练。");

  return {
    previousScore,
    delta,
    summary: delta > 0
      ? `比上次同题提高 ${delta} 分，复练有效。`
      : delta < 0
        ? `比上次同题低 ${Math.abs(delta)} 分，建议回看弱项后再录一次。`
        : "与上次同题分数持平，可以继续打磨细节。",
    notes: notes.slice(0, 4)
  };
}

function indexDetailScores(detailScores) {
  return detailScores.reduce((acc, item) => {
    acc[item.label] = Number(item.score);
    return acc;
  }, {});
}

function fingerprintText(text) {
  return normalizeWords(text).join(" ");
}

function getContentRecall(alignment, refWords) {
  const contentRef = getContentWords(refWords);
  if (!contentRef.length) return 1;
  const matchedContent = new Set(
    alignment
      .filter((item) => item.type === "match" && isContentWord(item.ref))
      .map((item) => item.ref)
  );
  return clamp01(matchedContent.size / new Set(contentRef).size);
}

function getContentWords(words) {
  return words.filter(isContentWord);
}

function isContentWord(word) {
  const stopWords = new Set([
    "a", "an", "the", "and", "or", "but", "to", "of", "in", "on", "at", "for", "from", "with", "by", "as",
    "is", "are", "was", "were", "be", "been", "being", "it", "this", "that", "these", "those", "i", "you",
    "we", "they", "he", "she", "my", "your", "our", "their", "his", "her", "do", "does", "did", "will",
    "would", "should", "can", "could", "may", "might", "have", "has", "had"
  ]);
  return word.length > 2 && !stopWords.has(word);
}

function hasAnyMarker(lowerText, markers) {
  return markers.some((marker) => lowerText.includes(marker));
}

function countMarkers(lowerText, markers) {
  return markers.reduce((sum, marker) => sum + (lowerText.includes(marker) ? 1 : 0), 0);
}

function countOverlap(words, wordSet) {
  return new Set(words).size ? [...new Set(words)].filter((word) => wordSet.has(word)).length : 0;
}

function getPromptDemandScore(prompt, signals) {
  const lowerPrompt = prompt.toLowerCase();
  const asksWhy = /\bwhy\b/.test(lowerPrompt);
  const asksPreference = /\bprefer|rather|better|should|would\b/.test(lowerPrompt);
  const asksDescribe = /\bdescribe|what|which|how\b/.test(lowerPrompt);
  let score = 0.45;
  if (!asksWhy && !asksPreference && !asksDescribe) score += 0.2;
  if (asksWhy && signals.hasReason) score += 0.28;
  if (asksPreference && signals.hasDirectAnswer) score += 0.22;
  if (asksDescribe && signals.hasExample) score += 0.2;
  if (signals.hasReason && signals.hasExample) score += 0.18;
  if (/\bshould\b/.test(lowerPrompt) && /\b(should|need|important|benefit|problem)\b/.test(signals.lower)) score += 0.12;
  return clamp01(score);
}

function getInterviewLengthScore(wordCount) {
  if (wordCount <= 0) return 0;
  if (wordCount < 25) return clamp01(wordCount / 35);
  if (wordCount <= 85) return clamp01(0.62 + (wordCount - 25) / 100);
  return clamp01(1 - (wordCount - 85) / 90);
}

function getSpecificDetailScore(transcript, words) {
  const lower = transcript.toLowerCase();
  const hasNumber = /\b(one|two|three|first|second|last|semester|week|year|\d+)\b/.test(lower);
  const hasClassContext = /\b(class|course|teacher|professor|project|presentation|library|campus|exam|homework|club)\b/.test(lower);
  const hasPastEvent = /\b(was|were|had|joined|took|made|helped|worked|prepared|studied|finished)\b/.test(lower);
  return clamp01((hasNumber ? 0.28 : 0) + (hasClassContext ? 0.36 : 0) + (hasPastEvent ? 0.26 : 0) + (words.length >= 55 ? 0.1 : 0));
}

function getAnswerShapeScore(words, lowerText) {
  const openingClear = words.slice(0, 12).some((word) => ["think", "prefer", "would", "yes", "no", "one", "my"].includes(word));
  const hasMiddle = hasAnyMarker(lowerText, [" because ", " for example ", " for instance ", " when i ", " once "]);
  const hasEnding = hasAnyMarker(lowerText, [" that is why ", " overall ", " in the end ", " as a result ", " this helps "]);
  return clamp01((openingClear ? 0.36 : 0) + (hasMiddle ? 0.42 : 0) + (hasEnding ? 0.22 : 0));
}

function getContentDensity(words) {
  if (!words.length) return 0;
  return getContentWords(words).length / words.length;
}

function getRepeatedWordPenalty(words) {
  const contentWords = getContentWords(words);
  if (contentWords.length < 12) return 0;
  const counts = contentWords.reduce((acc, word) => {
    acc[word] = (acc[word] || 0) + 1;
    return acc;
  }, {});
  const repeated = Object.values(counts).filter((count) => count >= 3).reduce((sum, count) => sum + count - 2, 0);
  return Math.min(0.18, repeated / Math.max(contentWords.length, 1));
}

function getFillerPenalty(text) {
  const matches = String(text).toLowerCase().match(/\b(um|uh|er|like|you know|sort of|kind of)\b/g) || [];
  return Math.min(0.22, matches.length * 0.045);
}

function getDurationFit(duration, minGood, maxGood) {
  if (!duration) return 0.58;
  if (duration >= minGood && duration <= maxGood) return 1;
  if (duration < minGood) return clamp01(duration / minGood);
  return clamp01(1 - (duration - maxGood) / maxGood);
}

function alignWords(refWords, hypWords) {
  const rows = refWords.length + 1;
  const cols = hypWords.length + 1;
  const dp = Array.from({ length: rows }, () => Array(cols).fill(0));
  const op = Array.from({ length: rows }, () => Array(cols).fill(""));

  for (let i = 1; i < rows; i += 1) {
    dp[i][0] = i;
    op[i][0] = "del";
  }
  for (let j = 1; j < cols; j += 1) {
    dp[0][j] = j;
    op[0][j] = "ins";
  }

  for (let i = 1; i < rows; i += 1) {
    for (let j = 1; j < cols; j += 1) {
      const same = refWords[i - 1] === hypWords[j - 1];
      const replaceCost = dp[i - 1][j - 1] + (same ? 0 : 1);
      const deleteCost = dp[i - 1][j] + 1;
      const insertCost = dp[i][j - 1] + 1;
      const min = Math.min(replaceCost, deleteCost, insertCost);
      dp[i][j] = min;
      op[i][j] = min === replaceCost ? (same ? "match" : "sub") : min === deleteCost ? "del" : "ins";
    }
  }

  const alignment = [];
  let i = refWords.length;
  let j = hypWords.length;
  while (i > 0 || j > 0) {
    const current = op[i][j];
    if (current === "match") {
      alignment.push({ type: "match", ref: refWords[i - 1], hyp: hypWords[j - 1] });
      i -= 1;
      j -= 1;
    } else if (current === "sub") {
      alignment.push({ type: "substitute", ref: refWords[i - 1], hyp: hypWords[j - 1] });
      i -= 1;
      j -= 1;
    } else if (current === "del") {
      alignment.push({ type: "missing", ref: refWords[i - 1], hyp: "" });
      i -= 1;
    } else {
      alignment.push({ type: "extra", ref: "", hyp: hypWords[j - 1] });
      j -= 1;
    }
  }

  return alignment.reverse();
}

function countAlignment(alignment) {
  return alignment.reduce(
    (acc, item) => {
      if (item.type === "match") acc.matches += 1;
      if (item.type === "substitute") acc.substitutions += 1;
      if (item.type === "missing") acc.missing += 1;
      if (item.type === "extra") acc.extras += 1;
      return acc;
    },
    { matches: 0, substitutions: 0, missing: 0, extras: 0 }
  );
}

function startFullRun() {
  stopIfRecording();
  const repeatSet = shuffle([...QUESTION_BANK.repeat]).slice(0, 6);
  const interviewSet = shuffle([...QUESTION_BANK.interview]).slice(0, 5);
  state.full = {
    active: true,
    complete: false,
    examMode: true,
    sequence: shuffle([...repeatSet, ...interviewSet]),
    index: 0,
    results: []
  };
  const current = state.full.sequence[0];
  state.mode = current.type;
  clearCurrentWork(true);
  state.tab = "full";
  render();
  window.setTimeout(() => speakPrompt(), 450);
}

function nextFullItem() {
  if (!state.feedback) {
    showToast("先完成本题分析，再进入下一题。");
    return;
  }

  if (state.full.index >= state.full.sequence.length - 1) {
    state.full.active = false;
    state.full.complete = true;
    clearCurrentWork(true);
    render();
    return;
  }

  state.full.index += 1;
  state.mode = state.full.sequence[state.full.index].type;
  clearCurrentWork(true);
  render();
  window.setTimeout(() => speakPrompt(), 450);
}

function getActiveQuestion(isFullRun) {
  if (isFullRun && state.full.sequence.length) {
    return state.full.sequence[state.full.index];
  }
  return state.mode === "repeat" ? state.currentRepeat : state.currentInterview;
}

function nextQuestion() {
  const isFull = state.tab === "full" && state.full.active;
  if (isFull) {
    const current = state.full.sequence[state.full.index];
    const pool = current.type === "repeat" ? QUESTION_BANK.repeat : QUESTION_BANK.interview;
    state.full.sequence[state.full.index] = randomItem(pool.filter((item) => item.id !== current.id));
    state.mode = state.full.sequence[state.full.index].type;
  } else if (state.mode === "repeat") {
    state.currentRepeat = randomItem(QUESTION_BANK.repeat.filter((item) => item.id !== state.currentRepeat.id));
  } else {
    state.currentInterview = randomItem(QUESTION_BANK.interview.filter((item) => item.id !== state.currentInterview.id));
  }
  clearCurrentWork(true);
  render();
}

function practiceQuestion(id) {
  const question = [...QUESTION_BANK.repeat, ...QUESTION_BANK.interview].find((item) => item.id === id);
  if (!question) return;
  stopIfRecording();
  if (question.type === "repeat") {
    state.currentRepeat = question;
    state.mode = "repeat";
  } else {
    state.currentInterview = question;
    state.mode = "interview";
  }
  state.full.active = false;
  state.full.complete = false;
  state.tab = "practice";
  clearCurrentWork(true);
  render();
}

function reviewHistory(id) {
  const item = loadHistory().find((entry) => entry.id === id);
  if (!item) return;
  const question = [...QUESTION_BANK.repeat, ...QUESTION_BANK.interview].find((entry) => entry.id === item.questionId) || {
    id: item.questionId,
    type: item.type,
    text: item.prompt,
    category: "History",
    difficulty: "Review",
    sample: ""
  };
  if (question.type === "repeat") {
    state.currentRepeat = question;
    state.mode = "repeat";
  } else {
    state.currentInterview = question;
    state.mode = "interview";
  }
  state.tab = "practice";
  state.transcript = item.transcript;
  state.duration = item.duration;
  state.elapsed = item.duration;
  state.feedback = question.type === "repeat"
    ? createRepeatFeedback(question, item.transcript, item.duration)
    : createInterviewFeedback(question, item.transcript, item.duration);
  state.status = "已载入历史记录";
  render();
}

function clearCurrentWork(keepQuestion) {
  if (!keepQuestion) {
    state.transcript = "";
  } else {
    state.transcript = "";
  }
  state.feedback = null;
  state.isScoring = false;
  state.status = "准备开始";
  state.elapsed = 0;
  state.duration = 0;
  state.audioStats = null;
  state.audioAnalysisPending = false;
  state.recognitionStats = {
    confidenceSamples: [],
    finalSegments: 0,
    interimSegments: 0
  };
  state.showSentence = false;
  state.lastSavedSignature = "";
  if (state.audioUrl) {
    URL.revokeObjectURL(state.audioUrl);
    state.audioUrl = "";
  }
  state.audioBlob = null;
}

function stopIfRecording() {
  if (state.isRecording) stopRecording();
  stopTimer();
}

function getFilteredQuestions() {
  return [...QUESTION_BANK.repeat, ...QUESTION_BANK.interview].filter((item) => {
    const typeMatch = state.bankType === "all" || item.type === state.bankType;
    const difficultyMatch = state.bankDifficulty === "all" || item.difficulty === state.bankDifficulty;
    return typeMatch && difficultyMatch;
  });
}

function loadHistory() {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    return raw ? JSON.parse(raw) : [];
  } catch (error) {
    return [];
  }
}

function saveHistoryItem(item) {
  const history = loadHistory();
  history.unshift(item);
  localStorage.setItem(STORAGE_KEY, JSON.stringify(history.slice(0, 50)));
}

function exportHistory() {
  const history = loadHistory();
  const blob = new Blob([JSON.stringify(history, null, 2)], { type: "application/json" });
  const url = URL.createObjectURL(blob);
  const anchor = document.createElement("a");
  anchor.href = url;
  anchor.download = "toefl-speaking-history.json";
  anchor.click();
  URL.revokeObjectURL(url);
}

function clearHistoryWithConfirm() {
  if (!loadHistory().length) return;
  const ok = window.confirm("确定清空本地历史记录吗？");
  if (!ok) return;
  localStorage.removeItem(STORAGE_KEY);
  render();
}

function getDefaultSetupConfig() {
  return {
    ai: {
      enabled: false,
      mode: "responses",
      endpoint: "",
      apiKey: "",
      model: "",
      temperature: 0.1
    },
    pronunciation: {
      enabled: false,
      provider: "azure",
      azureRegion: "",
      endpoint: "",
      apiKey: ""
    }
  };
}

function loadSetupConfig() {
  try {
    const saved = JSON.parse(localStorage.getItem(SETUP_STORAGE_KEY) || "{}");
    return mergeSetupConfig(getDefaultSetupConfig(), saved);
  } catch (error) {
    return getDefaultSetupConfig();
  }
}

function mergeSetupConfig(base, saved) {
  return {
    ai: { ...base.ai, ...(saved.ai || {}) },
    pronunciation: { ...base.pronunciation, ...(saved.pronunciation || {}) }
  };
}

function saveSetupFromForm() {
  const setup = {
    ai: {
      enabled: Boolean(document.querySelector("#aiEnabled")?.checked),
      mode: document.querySelector("#aiMode")?.value || "responses",
      endpoint: cleanupSpacing(document.querySelector("#aiEndpoint")?.value || ""),
      apiKey: document.querySelector("#aiKey")?.value || "",
      model: cleanupSpacing(document.querySelector("#aiModel")?.value || ""),
      temperature: Number(document.querySelector("#aiTemperature")?.value || 0.1)
    },
    pronunciation: {
      enabled: Boolean(document.querySelector("#pronEnabled")?.checked),
      provider: document.querySelector("#pronProvider")?.value || "azure",
      azureRegion: cleanupSpacing(document.querySelector("#azureRegion")?.value || ""),
      endpoint: cleanupSpacing(document.querySelector("#pronEndpoint")?.value || ""),
      apiKey: document.querySelector("#pronKey")?.value || ""
    }
  };
  state.setup = mergeSetupConfig(getDefaultSetupConfig(), setup);
  localStorage.setItem(SETUP_STORAGE_KEY, JSON.stringify(state.setup));
  showToast("设置已保存到当前浏览器。");
}

function clearSetupWithConfirm() {
  const ok = window.confirm("确定清空本地 API 设置吗？");
  if (!ok) return;
  localStorage.removeItem(SETUP_STORAGE_KEY);
  state.setup = getDefaultSetupConfig();
  render();
}

function averageScore(items) {
  const scored = items.filter((item) => Number.isFinite(Number(item.score)));
  if (!scored.length) return "";
  const average = scored.reduce((sum, item) => sum + Number(item.score), 0) / scored.length;
  return String(roundHalf(average));
}

function normalizeWords(text) {
  return cleanupSpacing(text)
    .toLowerCase()
    .replace(/[’]/g, "'")
    .replace(/[^a-z0-9'\s-]/g, " ")
    .split(/\s+/)
    .map((word) => word.replace(/^-+|-+$/g, ""))
    .filter(Boolean);
}

function cleanupSpacing(text) {
  return String(text || "").replace(/\s+/g, " ").trim();
}

function formatTime(seconds) {
  const safe = Math.max(0, Number(seconds) || 0);
  const minutes = Math.floor(safe / 60);
  const rest = safe % 60;
  return `${String(minutes).padStart(2, "0")}:${String(rest).padStart(2, "0")}`;
}

function formatDate(value) {
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return "";
  return `${date.getMonth() + 1}/${date.getDate()} ${String(date.getHours()).padStart(2, "0")}:${String(date.getMinutes()).padStart(2, "0")}`;
}

function isToday(value) {
  const date = new Date(value);
  const now = new Date();
  return date.getFullYear() === now.getFullYear() && date.getMonth() === now.getMonth() && date.getDate() === now.getDate();
}

function randomItem(items) {
  return items[Math.floor(Math.random() * items.length)];
}

function shuffle(items) {
  const copy = [...items];
  for (let index = copy.length - 1; index > 0; index -= 1) {
    const swapIndex = Math.floor(Math.random() * (index + 1));
    [copy[index], copy[swapIndex]] = [copy[swapIndex], copy[index]];
  }
  return copy;
}

function clamp(min, max, value) {
  return Math.max(min, Math.min(max, value));
}

function clamp01(value) {
  return clamp(0, 1, value);
}

function percentile(values, ratio) {
  if (!values.length) return 0;
  const sorted = [...values].sort((a, b) => a - b);
  const index = Math.max(0, Math.min(sorted.length - 1, Math.floor((sorted.length - 1) * ratio)));
  return sorted[index];
}

function roundHalf(value) {
  return Math.round(Number(value) * 2) / 2;
}

function escapeHtml(value) {
  return String(value)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;")
    .replace(/'/g, "&#039;");
}

function showToast(message) {
  state.toast = message;
  render();
  window.setTimeout(() => {
    state.toast = "";
    render();
  }, 2600);
}

init();
